# 法令番号の非同期解決 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 法令番号を伴う他法令への参照を、e-Gov への問い合わせで lawId に解決してリンクにする。法律の lawId は提出区分を含むため法令番号から導出できず、現在は無リンクになっている。

**Architecture:** 非同期を `segmentReferenceLinks` の前段に閉じ込める。法令本文をロードした後、フックが本文中の法令番号を走査・重複排除して解決し、`法令番号キー → lawId` の対応表をコンテキストへ渡す。`segmentReferenceLinks` と `detectCrossLawSpan` は純粋・同期のまま、表を引くだけにする。解決できたものから順に表へ載るので、リンクは逐次増える。

**Tech Stack:** TypeScript 6 / React 19 / Vitest / IndexedDB (idb)

**設計:** [docs/superpowers/specs/20260831\_法令番号の非同期解決.md](../specs/20260831_法令番号の非同期解決.md)

## Global Constraints

- コード内のコメントとコミットメッセージは日本語。ログとエラーメッセージは英語
- コミットメッセージは Conventional Commits 形式。末尾に `Co-Authored-By: Claude Fable 6 <noreply@anthropic.com>` を置く
- 検証コマンドは `pnpm run typecheck` / `pnpm run lint` / `pnpm exec prettier src --check` / `pnpm exec vitest run --dir src`
- vitest は必ず `--dir src` を付ける。付けないと `.claude/worktrees` 配下を拾う
- pnpm は `mise exec` を経由せず直接呼ぶ
- **lint の指摘は設定変更やコメント抑制で消さない。`eslint-disable` を書かない**
- lint や format の設定ファイルは変更しない
- ソースコードを文字列探索して通すだけのテストは書かない
- 純粋関数のテストは `it.each` による table testing にする
- `git commit --no-verify` を使う（pre-commit フックが `.git/info/exclude` の汚染で誤検知するため）
- 既存の 5 件のテスト失敗（`src/core/settings/display-preferences` 系と `storage-limit`）と 4 件の lint warning（`src/app/search-palette-context.tsx`）は `origin/main` 由来でスコープ外
- ネットワークに出るテストを書かない。`createEgovLawRepository({ fetcher })` に stub を渡す
- IndexedDB のスキーマは変更しない。既存の `lawCatalog` ストアをそのまま使う

---

### Task 1: 法令番号のキーと e-Gov の構造化クエリ

**Files:**

- Modify: `src/core/jump/law-number.ts`
- Modify: `src/core/jump/law-number.test.ts`
- Modify: `src/core/jump/index.ts`
- Modify: `src/core/egov/repository.ts`
- Modify: `src/core/egov/egovLawRepository.test.ts`

**Interfaces:**

- Consumes: 既存の `ParsedLawNumber { era; year; type; number }` と `parseLawNumber`
- Produces:
  - `lawNumberKey(parsed: ParsedLawNumber): string` — `"Heisei/11/法律/156"` の形
  - `lawNumberTypeCode(type: string): string | undefined` — `"法律" → "Act"`、`"政令" → "CabinetOrder"`、対応が無ければ `undefined`
  - `LawListQuery` に `lawNumberEra?: string` / `lawNumberYear?: number` / `lawNumberType?: string` / `lawNumberNumber?: number`

- [ ] **Step 1: 失敗するテストを書く**

`src/core/jump/law-number.test.ts` の末尾に追加する。

```ts
describe("lawNumberKey", () => {
  it("builds the same key from kanji and arabic notations", () => {
    const kanji = parseLawNumber("平成十一年法律第百五十六号");
    const arabic = parseLawNumber("平成11年法律第156号");

    expect(kanji).toBeDefined();
    expect(arabic).toBeDefined();
    expect(lawNumberKey(kanji!)).toBe(lawNumberKey(arabic!));
  });

  it.each([
    {
      name: "keys an act by era, year, type and number",
      parsed: { era: "Heisei", year: 11, type: "法律", number: 156 },
      expected: "Heisei/11/法律/156",
    },
    {
      name: "keys a cabinet order distinctly from an act of the same number",
      parsed: { era: "Heisei", year: 11, type: "政令", number: 156 },
      expected: "Heisei/11/政令/156",
    },
  ] as const)("$name", ({ expected, parsed }) => {
    expect(lawNumberKey(parsed)).toBe(expected);
  });
});

describe("lawNumberTypeCode", () => {
  it.each([
    { name: "maps 法律 to Act", type: "法律", expected: "Act" },
    { name: "maps 政令 to CabinetOrder", type: "政令", expected: "CabinetOrder" },
    { name: "does not map a ministerial ordinance", type: "大蔵省令", expected: undefined },
    { name: "does not map a dajokan proclamation", type: "太政官布告", expected: undefined },
  ])("$name", ({ expected, type }) => {
    expect(lawNumberTypeCode(type)).toBe(expected);
  });
});
```

ファイル先頭の import を差し替える。

```ts
import {
  deriveLawIdFromLawNumber,
  lawNumberKey,
  lawNumberTypeCode,
  parseLawNumber,
} from "./law-number";
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
pnpm exec vitest run --dir src src/core/jump/law-number.test.ts
```

Expected: FAIL（`lawNumberKey` が export されていない）

- [ ] **Step 3: 実装する**

`src/core/jump/law-number.ts` の末尾に追加する。

```ts
// 法令番号の突き合わせキー。表記が漢数字と算用数字で割れるため、パース結果から作る。
// e-Gov が返す law_num（常に漢数字）も parseLawNumber を通して同じキーにする。
export const lawNumberKey = (parsed: ParsedLawNumber): string =>
  `${parsed.era}/${String(parsed.year)}/${parsed.type}/${String(parsed.number)}`;

// 種別語から e-Gov の law_num_type へ。対応の無い種別（府令・省令・太政官布告など）は
// 問い合わせても引けないため undefined を返し、解決を試みない。
const lawNumberTypeCodes = new Map([
  ["法律", "Act"],
  ["政令", "CabinetOrder"],
]);

export const lawNumberTypeCode = (type: string): string | undefined => lawNumberTypeCodes.get(type);
```

`src/core/jump/index.ts` の `law-number` の export に足す。

```ts
export {
  deriveLawIdFromLawNumber,
  lawNumberKey,
  lawNumberTypeCode,
  parseLawNumber,
} from "./law-number";
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
pnpm exec vitest run --dir src src/core/jump/law-number.test.ts
```

Expected: PASS

- [ ] **Step 5: e-Gov の構造化クエリのテストを書く**

`src/core/egov/egovLawRepository.test.ts` の `describe("EgovLawRepository")` の中に追加する。

```ts
it("sends the structured law number query", async () => {
  const { calls, fetcher } = createJsonFetchStub(lawsFixture);
  const repository = createEgovLawRepository({ fetcher, now });

  await repository.listLaws({
    lawNumberEra: "Showa",
    lawNumberYear: 32,
    lawNumberType: "Act",
    lawNumberNumber: 166,
    limit: 1,
  });

  expect(calls[0]?.input).toBe(
    "https://laws.e-gov.go.jp/api/2/laws?law_num_era=Showa&law_num_year=32&law_num_type=Act&law_num_num=166&limit=1&response_format=json",
  );
});
```

- [ ] **Step 6: テストが失敗することを確認する**

```bash
pnpm exec vitest run --dir src src/core/egov/egovLawRepository.test.ts
```

Expected: FAIL（クエリに `law_num_era` 等が乗らない）

パラメータの順序が違って落ちた場合は、実装後の実際の URL に合わせて期待値を直してよい。`buildUrl` は渡した順に積むため、Step 7 の追加位置で決まる。

- [ ] **Step 7: 実装する**

`src/core/egov/repository.ts` の `LawListQuery` に足す。

```ts
export interface LawListQuery {
  lawId?: string;
  lawNumber?: string;
  // 法令番号を構造化して渡す。law_num の完全一致と違い、表記の揺れを気にせず一意に引ける。
  lawNumberEra?: string;
  lawNumberYear?: number;
  lawNumberType?: string;
  lawNumberNumber?: number;
  title?: string;
  lawTypes?: string[];
  asOf?: string;
  offset?: number;
  limit?: number;
}
```

`listLaws` のクエリ組み立てに足す。`law_num` の直後に置く。

```ts
          law_id: query.lawId,
          law_num: query.lawNumber,
          law_num_era: query.lawNumberEra,
          law_num_year: query.lawNumberYear,
          law_num_type: query.lawNumberType,
          law_num_num: query.lawNumberNumber,
          law_title: query.title,
```

- [ ] **Step 8: テストが通ることを確認する**

```bash
pnpm exec vitest run --dir src src/core/egov/egovLawRepository.test.ts
```

Expected: PASS

- [ ] **Step 9: 検証してコミットする**

```bash
pnpm run typecheck && pnpm run lint && pnpm exec prettier src --check && pnpm exec vitest run --dir src
```

```bash
git add src/core/jump/law-number.ts src/core/jump/law-number.test.ts src/core/jump/index.ts src/core/egov/repository.ts src/core/egov/egovLawRepository.test.ts
git commit --no-verify -m "feat: 法令番号のキーと e-Gov の構造化クエリを足す

Co-Authored-By: Claude Fable 6 <noreply@anthropic.com>"
```

---

### Task 2: 法令番号の解決器

**Files:**

- Create: `src/core/jump/law-number-resolver.ts`
- Create: `src/core/jump/law-number-resolver.test.ts`
- Modify: `src/core/jump/index.ts`

**Interfaces:**

- Consumes: `lawNumberKey` / `lawNumberTypeCode` / `deriveLawIdFromLawNumber` / `parseLawNumber`（Task 1）、`LawRepository`（`@/core/egov`）、`SearchIndexRepository`（`@/core/search`）
- Produces:
  - `interface LawNumberResolver { resolve(parsed: ParsedLawNumber, options?: { signal?: AbortSignal }): Promise<string | undefined> }`
  - `createLawNumberResolver(dependencies: { lawRepository: LawRepository; indexRepository: SearchIndexRepository }): LawNumberResolver`

- [ ] **Step 1: 失敗するテストを書く**

`src/core/jump/law-number-resolver.test.ts` を新規作成する。

```ts
import type { LawListResult, LawRepository } from "@/core/egov";
import type { SearchIndexRepository } from "@/core/search";
import type { LawCatalogEntry } from "@/core/storage";
import { describe, expect, it, vi } from "vitest";

import { createLawNumberResolver } from "./law-number-resolver";
import { parseLawNumber } from "./law-number";

const parse = (text: string) => {
  const parsed = parseLawNumber(text);

  if (parsed === undefined) {
    throw new Error(`unparsable law number: ${text}`);
  }

  return parsed;
};

const emptyResult: LawListResult = { totalCount: 0, count: 0, laws: [] };

const lawResult = (lawId: string, lawNumber: string): LawListResult => ({
  totalCount: 1,
  count: 1,
  laws: [
    {
      law: { lawId, title: "テスト法", lawNumber, lawType: "Act", aliases: [], source: "egov" },
      revision: { lawId, revisionId: `${lawId}_r`, fetchedAt: "2026-08-31T00:00:00.000Z" },
    },
  ],
});

const createDependencies = ({
  catalog = [],
  listLaws = () => Promise.resolve(emptyResult),
}: {
  catalog?: LawCatalogEntry[];
  listLaws?: LawRepository["listLaws"];
} = {}) => {
  const upsertCatalogEntries = vi.fn<SearchIndexRepository["upsertCatalogEntries"]>(() =>
    Promise.resolve(),
  );
  const listLawsSpy = vi.fn(listLaws);

  return {
    upsertCatalogEntries,
    listLawsSpy,
    dependencies: {
      lawRepository: { listLaws: listLawsSpy } as unknown as LawRepository,
      indexRepository: {
        listCatalog: () => Promise.resolve(catalog),
        upsertCatalogEntries,
      } as unknown as SearchIndexRepository,
    },
  };
};

describe("createLawNumberResolver", () => {
  it("derives a cabinet order without touching the network", async () => {
    const { dependencies, listLawsSpy } = createDependencies();
    const resolver = createLawNumberResolver(dependencies);

    await expect(resolver.resolve(parse("昭和二十二年政令第二十一号"))).resolves.toBe(
      "322CO0000000021",
    );
    expect(listLawsSpy).not.toHaveBeenCalled();
  });

  it("resolves from the cached catalog without touching the network", async () => {
    const { dependencies, listLawsSpy } = createDependencies({
      catalog: [
        {
          lawId: "332AC1000000166",
          title: "核原料物質、核燃料物質及び原子炉の規制に関する法律",
          lawNumber: "昭和三十二年法律第百六十六号",
          aliases: [],
          cachedAt: "2026-08-31T00:00:00.000Z",
        },
      ],
    });
    const resolver = createLawNumberResolver(dependencies);

    // 本文側は算用数字で現れることがある。キーで突き合わせるため引けなければならない。
    await expect(resolver.resolve(parse("昭和32年法律第166号"))).resolves.toBe("332AC1000000166");
    expect(listLawsSpy).not.toHaveBeenCalled();
  });

  it("resolves an act through the repository and caches the result", async () => {
    const { dependencies, listLawsSpy, upsertCatalogEntries } = createDependencies({
      listLaws: () => Promise.resolve(lawResult("332AC1000000166", "昭和三十二年法律第百六十六号")),
    });
    const resolver = createLawNumberResolver(dependencies);

    await expect(resolver.resolve(parse("昭和32年法律第166号"))).resolves.toBe("332AC1000000166");
    expect(listLawsSpy).toHaveBeenCalledWith(
      {
        lawNumberEra: "Showa",
        lawNumberYear: 32,
        lawNumberType: "Act",
        lawNumberNumber: 166,
        limit: 1,
      },
      {},
    );
    expect(upsertCatalogEntries).toHaveBeenCalledWith([
      expect.objectContaining({ lawId: "332AC1000000166" }),
    ]);
  });

  it.each([
    {
      name: "does not resolve when the repository returns more than one law",
      result: { totalCount: 2, count: 1, laws: lawResult("332AC1000000166", "x").laws },
    },
    { name: "does not resolve when the repository returns nothing", result: emptyResult },
  ])("$name", async ({ result }) => {
    const { dependencies } = createDependencies({ listLaws: () => Promise.resolve(result) });
    const resolver = createLawNumberResolver(dependencies);

    await expect(resolver.resolve(parse("昭和32年法律第166号"))).resolves.toBeUndefined();
  });

  it("does not query a law type the api cannot look up", async () => {
    const { dependencies, listLawsSpy } = createDependencies();
    const resolver = createLawNumberResolver(dependencies);

    await expect(resolver.resolve(parse("明治二十七年大蔵省令第二号"))).resolves.toBeUndefined();
    expect(listLawsSpy).not.toHaveBeenCalled();
  });

  it("does not retry a law number that failed to resolve", async () => {
    const { dependencies, listLawsSpy } = createDependencies({
      listLaws: () => Promise.resolve(emptyResult),
    });
    const resolver = createLawNumberResolver(dependencies);

    await resolver.resolve(parse("昭和32年法律第166号"));
    await resolver.resolve(parse("昭和32年法律第166号"));

    expect(listLawsSpy).toHaveBeenCalledTimes(1);
  });

  it("does not remember an abort as a failure", async () => {
    const abortError = new DOMException("aborted", "AbortError");
    let attempts = 0;
    const { dependencies } = createDependencies({
      listLaws: () => {
        attempts += 1;

        return attempts === 1
          ? Promise.reject(abortError)
          : Promise.resolve(lawResult("332AC1000000166", "昭和三十二年法律第百六十六号"));
      },
    });
    const resolver = createLawNumberResolver(dependencies);

    await expect(resolver.resolve(parse("昭和32年法律第166号"))).rejects.toBe(abortError);
    await expect(resolver.resolve(parse("昭和32年法律第166号"))).resolves.toBe("332AC1000000166");
  });
});
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
pnpm exec vitest run --dir src src/core/jump/law-number-resolver.test.ts
```

Expected: FAIL（`Failed to resolve import "./law-number-resolver"`）

- [ ] **Step 3: 実装する**

`src/core/jump/law-number-resolver.ts` を新規作成する。

```ts
import type { LawRepository } from "@/core/egov";
import type { SearchIndexRepository } from "@/core/search";
import type { LawCatalogEntry } from "@/core/storage";

import {
  deriveLawIdFromLawNumber,
  lawNumberKey,
  lawNumberTypeCode,
  parseLawNumber,
  type ParsedLawNumber,
} from "./law-number";

export interface LawNumberResolver {
  // キャッシュ優先で lawId を返す。解決できなければ undefined。
  resolve(parsed: ParsedLawNumber, options?: { signal?: AbortSignal }): Promise<string | undefined>;
}

export interface LawNumberResolverDependencies {
  lawRepository: LawRepository;
  indexRepository: SearchIndexRepository;
}

// fetch の中断は DOMException("AbortError")。名前で判定する（catalog.ts と同じ方針）。
const isAbortError = (error: unknown): boolean =>
  error instanceof DOMException && error.name === "AbortError";

const toCatalogEntry = (law: {
  lawId: string;
  title: string;
  lawNumber?: string;
  lawType?: string;
  aliases: string[];
}): LawCatalogEntry => ({
  lawId: law.lawId,
  title: law.title,
  ...(law.lawNumber === undefined ? {} : { lawNumber: law.lawNumber }),
  ...(law.lawType === undefined ? {} : { lawType: law.lawType }),
  aliases: law.aliases,
  cachedAt: new Date().toISOString(),
});

export const createLawNumberResolver = ({
  indexRepository,
  lawRepository,
}: LawNumberResolverDependencies): LawNumberResolver => {
  // 解決済みと、解決できなかったものの記憶。同じ法令番号を何度も問い合わせない。
  const resolved = new Map<string, string>();
  const unresolvable = new Set<string>();
  // 起動から一度だけカタログを読む。法令を切り替えるたびに読み直さない。
  let cachedCatalog: Promise<Map<string, string>> | undefined;

  const loadCatalogIndex = async (): Promise<Map<string, string>> => {
    const entries = await indexRepository.listCatalog();
    const index = new Map<string, string>();

    for (const entry of entries) {
      if (entry.lawNumber === undefined) {
        continue;
      }

      const parsed = parseLawNumber(entry.lawNumber);

      if (parsed !== undefined) {
        index.set(lawNumberKey(parsed), entry.lawId);
      }
    }

    return index;
  };

  return {
    async resolve(parsed, options = {}) {
      const derived = deriveLawIdFromLawNumber(parsed);

      if (derived !== undefined) {
        return derived;
      }

      const key = lawNumberKey(parsed);
      const known = resolved.get(key);

      if (known !== undefined) {
        return known;
      }

      if (unresolvable.has(key)) {
        return undefined;
      }

      // キャッシュは種別によらず引く。省令のように法令番号から問い合わせられない種別でも、
      // 検索経由でカタログに入っていれば解決できる。
      cachedCatalog ??= loadCatalogIndex();
      const cachedLawId = (await cachedCatalog).get(key);

      if (cachedLawId !== undefined) {
        resolved.set(key, cachedLawId);
        return cachedLawId;
      }

      const typeCode = lawNumberTypeCode(parsed.type);

      if (typeCode === undefined) {
        // 府令・省令などは法令番号から引けない。問い合わせずに諦める。
        unresolvable.add(key);
        return undefined;
      }

      let result;

      try {
        result = await lawRepository.listLaws(
          {
            lawNumberEra: parsed.era,
            lawNumberYear: parsed.year,
            lawNumberType: typeCode,
            lawNumberNumber: parsed.number,
            limit: 1,
          },
          options.signal === undefined ? {} : { signal: options.signal },
        );
      } catch (error) {
        // 中断は正常系。失敗として覚えず、呼び出し側へ伝播させる。
        if (isAbortError(error)) {
          throw error;
        }

        // ネットワーク不通などは失敗として覚え、同じセッション中は再試行しない。
        unresolvable.add(key);
        return undefined;
      }

      const law = result.laws[0]?.law;

      // 一意に引けたときだけ採る。複数ヒットはどれか決められない。
      if (result.totalCount !== 1 || law === undefined) {
        unresolvable.add(key);
        return undefined;
      }

      resolved.set(key, law.lawId);
      await indexRepository.upsertCatalogEntries([toCatalogEntry(law)]);

      return law.lawId;
    },
  };
};
```

`src/core/jump/index.ts` に export を足す。

```ts
export { createLawNumberResolver } from "./law-number-resolver";
export type { LawNumberResolver, LawNumberResolverDependencies } from "./law-number-resolver";
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
pnpm exec vitest run --dir src src/core/jump/law-number-resolver.test.ts
```

Expected: PASS（8 tests）

- [ ] **Step 5: 検証してコミットする**

```bash
pnpm run typecheck && pnpm run lint && pnpm exec prettier src --check && pnpm exec vitest run --dir src
```

```bash
git add src/core/jump/law-number-resolver.ts src/core/jump/law-number-resolver.test.ts src/core/jump/index.ts
git commit --no-verify -m "feat: 法令番号から lawId を解決する解決器を足す

Co-Authored-By: Claude Fable 6 <noreply@anthropic.com>"
```

---

### Task 3: 対応表をリンク解決へ通す

**Files:**

- Modify: `src/core/viewer/cross-law-span.ts`
- Modify: `src/core/viewer/cross-law-span.test.ts`
- Modify: `src/core/viewer/reference-links.ts`
- Modify: `src/core/viewer/reference-links.test.ts`
- Modify: `src/core/viewer/LawNodeList.tsx`
- Modify: `src/core/viewer/LawNodeList.test.tsx`
- Modify: `src/core/viewer/LawDocumentView.tsx`

**Interfaces:**

- Consumes: `lawNumberKey`（Task 1）
- Produces: `ArticleLinkContext` に `lawIdByLawNumber?: ReadonlyMap<string, string>`。`LawNodeList` と `LawDocumentView` に同名の prop

- [ ] **Step 1: 失敗するテストを書く**

`src/core/viewer/cross-law-span.test.ts` の `detect` ヘルパーを、対応表を渡せるように差し替える。既存の呼び出しは引数を足さないので影響しない。

```ts
const detect = (
  text: string,
  marker: string,
  minIndex = 0,
  lawIdByLawNumber?: ReadonlyMap<string, string>,
) => detectCrossLawSpan(text, text.indexOf(marker), minIndex, lawIdByLawNumber);
```

同ファイルの末尾に追加する。

```ts
describe("detectCrossLawSpan with resolved law numbers", () => {
  // 法律の lawId は提出区分を含むため法令番号から導出できない。解決済みの対応表から引く。
  const lawIdByLawNumber = new Map([["Heisei/11/法律/156", "411AC0000000156"]]);

  it("resolves an act through the resolved law number map", () => {
    expect(
      detect("原子力災害対策特別措置法（平成11年法律第156号）第2条", "第2条", 0, lawIdByLawNumber),
    ).toEqual({ startIndex: 0, lawId: "411AC0000000156" });
  });

  it("keeps an act unresolved when the map has no entry", () => {
    expect(
      detect("不正競争防止法（平成5年法律第47号）第2条", "第2条", 0, lawIdByLawNumber),
    ).toEqual({ startIndex: 0 });
  });
});
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
pnpm exec vitest run --dir src src/core/viewer/cross-law-span.test.ts
```

Expected: FAIL（`{ startIndex: 0 }` が返る。対応表を見ていない）

- [ ] **Step 3: `detectCrossLawSpan` に対応表を通す**

`src/core/viewer/cross-law-span.ts` の import に `lawNumberKey` を足す。

```ts
import {
  createAliasResolver,
  deriveLawIdFromLawNumber,
  initialAliasDictionary,
  lawNumberKey,
  parseLawNumber,
} from "@/core/jump";
```

`readLawNumberParenthesis` が対応表も見るようにする。関数のシグネチャと、`lawId` を決める箇所を差し替える。

```ts
const readLawNumberParenthesis = (
  text: string,
  matchStart: number,
  minIndex: number,
  lawIdByLawNumber: ReadonlyMap<string, string> | undefined,
): LawNumberParenthesis | undefined => {
```

`parseLawNumber` の結果から `lawId` を決める箇所を差し替える。

```ts
const lawId = deriveLawIdFromLawNumber(parsed) ?? lawIdByLawNumber?.get(lawNumberKey(parsed));

return { startIndex: index, ...(lawId === undefined ? {} : { lawId }) };
```

`detectCrossLawSpan` のシグネチャと呼び出しを差し替える。

```ts
export const detectCrossLawSpan = (
  text: string,
  matchStart: number,
  minIndex: number,
  lawIdByLawNumber?: ReadonlyMap<string, string>,
): CrossLawSpan | undefined => {
  const parenthesis = readLawNumberParenthesis(text, matchStart, minIndex, lawIdByLawNumber);
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
pnpm exec vitest run --dir src src/core/viewer/cross-law-span.test.ts
```

Expected: PASS

- [ ] **Step 5: `segmentReferenceLinks` のテストを書く**

`src/core/viewer/reference-links.test.ts` の末尾に追加する。

```ts
describe("segmentReferenceLinks with resolved law numbers", () => {
  const articles = buildArticleLinkEntries(lawNodes);
  const headings = buildHeadingLinkEntries(lawNodes);

  const linkTexts = (text: string, context: Partial<ArticleLinkContext> = {}) =>
    segmentReferenceLinks(text, { articles, headings, ...context })
      .filter((segment) => segment.kind === "link")
      .map((segment) => segment.text);

  const lawIdByLawNumber = new Map([["Heisei/11/法律/156", "411AC0000000156"]]);

  it("links an act once its law number is resolved", () => {
    expect(
      segmentReferenceLinks("原子力災害対策特別措置法（平成11年法律第156号）第2条の規定", {
        articles,
        headings,
        currentArticleNumber: "16",
        lawIdByLawNumber,
      }).filter((segment) => segment.kind === "link"),
    ).toEqual([
      {
        kind: "link",
        text: "原子力災害対策特別措置法（平成11年法律第156号）第2条",
        target: { kind: "article", lawId: "411AC0000000156", articleNumber: "2" },
      },
    ]);
  });

  it("keeps the act unlinked without the resolved law number", () => {
    expect(
      linkTexts("原子力災害対策特別措置法（平成11年法律第156号）第2条の規定", {
        currentArticleNumber: "16",
      }),
    ).toEqual([]);
  });
});
```

- [ ] **Step 6: テストが失敗することを確認する**

```bash
pnpm exec vitest run --dir src src/core/viewer/reference-links.test.ts
```

Expected: FAIL（`lawIdByLawNumber` が `ArticleLinkContext` に無く型エラー、またはリンクが空）

- [ ] **Step 7: `ArticleLinkContext` に足して渡す**

`src/core/viewer/reference-links.ts` の `ArticleLinkContext` に追加する。

```ts
export interface ArticleLinkContext {
  articles: ArticleLinkEntry[];
  headings: HeadingLinkEntry[];
  // 前条・次条・前項の基準となる現在位置。
  currentArticleNumber?: string;
  currentParagraphNumber?: string;
  // 編を伴わない「第2章」を現在の編の中で解決するための祖先文脈。
  // 前編・次編・前章・次章の基準にもなる。
  currentPartNumber?: string;
  currentChapterNumber?: string;
  // 法令番号キー → lawId。法律の lawId は法令番号から導出できないため、
  // 前段で非同期に解決した結果をここから引く。
  lawIdByLawNumber?: ReadonlyMap<string, string>;
}
```

`segmentReferenceLinks` の中の `detectCrossLawSpan` 呼び出しに引数を足す。

```ts
const crossLawSpan = detectCrossLawSpan(text, match.index, lastIndex, context.lawIdByLawNumber);
```

- [ ] **Step 8: テストが通ることを確認する**

```bash
pnpm exec vitest run --dir src src/core/viewer/reference-links.test.ts
```

Expected: PASS

- [ ] **Step 9: 描画側のテストを書く**

`src/core/viewer/LawNodeList.test.tsx` の `describe("reference links")` の中に追加する。

```ts
    it("links an act through the resolved law number map", () => {
      const actNodes: LawNode[] = referenceNodes.map((existing) =>
        existing.id === "article:16/paragraph:1"
          ? {
              ...existing,
              plainText: "原子力災害対策特別措置法（平成11年法律第156号）第2条の規定による。",
              rawText: "",
            }
          : existing,
      );

      render(
        <LawNodeList
          lawId="129AC0000000089"
          lawIdByLawNumber={new Map([["Heisei/11/法律/156", "411AC0000000156"]])}
          nodes={actNodes}
        />,
      );

      expect(
        screen.getByRole("link", {
          name: "原子力災害対策特別措置法（平成11年法律第156号）第2条",
        }),
      ).toHaveAttribute("href", "/laws/411AC0000000156/articles/2");
    });
```

- [ ] **Step 10: テストが失敗することを確認する**

```bash
pnpm exec vitest run --dir src src/core/viewer/LawNodeList.test.tsx
```

Expected: FAIL（`lawIdByLawNumber` prop が無く型エラー）

- [ ] **Step 11: prop を通す**

`src/core/viewer/LawNodeList.tsx` の `LawNodeListProps` に足す。

```ts
  lawIdByLawNumber?: ReadonlyMap<string, string>;
```

引数の分割代入に `lawIdByLawNumber,` を足し、`LinkingOptions` を作る `useMemo` に載せる。

```ts
    () => ({
      articles,
      displayMode,
      headings,
      lawId,
      lawIdByLawNumber,
      onSelectArticle,
      onSelectCrossLawArticle,
    }),
    [
      articles,
      displayMode,
      headings,
      lawId,
      lawIdByLawNumber,
      onSelectArticle,
      onSelectCrossLawArticle,
    ],
```

`LinkingOptions` の型に足す。

```ts
lawIdByLawNumber: ReadonlyMap<string, string> | undefined;
```

`renderLinkedText` の中の `segmentReferenceLinks` 呼び出しに足す。

既存の呼び出しは編・章の文脈も渡している。**行を消さずに `lawIdByLawNumber` の 3 行だけを足す。**

```ts
const segments = segmentReferenceLinks(text, {
  articles: linking.articles,
  headings: linking.headings,
  ...(linking.lawIdByLawNumber === undefined ? {} : { lawIdByLawNumber: linking.lawIdByLawNumber }),
  ...(position.partNumber === undefined ? {} : { currentPartNumber: position.partNumber }),
  ...(position.chapterNumber === undefined ? {} : { currentChapterNumber: position.chapterNumber }),
  ...(position.articleNumber === undefined ? {} : { currentArticleNumber: position.articleNumber }),
  ...(position.paragraphNumber === undefined
    ? {}
    : { currentParagraphNumber: position.paragraphNumber }),
});
```

`src/core/viewer/LawDocumentView.tsx` にも同じ prop を足し、`LawNodeList` へ渡す。

```ts
  lawIdByLawNumber?: ReadonlyMap<string, string>;
```

```tsx
lawIdByLawNumber = { lawIdByLawNumber };
```

- [ ] **Step 12: テストが通ることを確認する**

```bash
pnpm exec vitest run --dir src src/core/viewer/LawNodeList.test.tsx
```

Expected: PASS

- [ ] **Step 13: 検証してコミットする**

```bash
pnpm run typecheck && pnpm run lint && pnpm exec prettier src --check && pnpm exec vitest run --dir src
```

```bash
git add src/core/viewer
git commit --no-verify -m "feat: 解決済みの法令番号を本文リンクへ通す

Co-Authored-By: Claude Fable 6 <noreply@anthropic.com>"
```

---

### Task 4: 本文の走査と逐次解決

**Files:**

- Create: `src/app/use-cross-law-numbers.ts`
- Create: `src/app/use-cross-law-numbers.test.ts`
- Create: `src/app/law-number-resolver.ts`
- Modify: `src/app/law-viewer-page.tsx`

**Interfaces:**

- Consumes: `createLawNumberResolver` / `LawNumberResolver`（Task 2）、`parseLawNumber` / `lawNumberKey`（Task 1）、`LawDocumentView` の `lawIdByLawNumber` prop（Task 3）
- Produces: `useCrossLawNumbers(nodes: LawNode[], resolver: LawNumberResolver): ReadonlyMap<string, string>`

- [ ] **Step 1: 失敗するテストを書く**

`src/app/use-cross-law-numbers.test.ts` を新規作成する。

```ts
import type { LawNode } from "@/core/domain";
import type { LawNumberResolver } from "@/core/jump";
import { renderHook, waitFor } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";

import { useCrossLawNumbers } from "./use-cross-law-numbers";

const node = (id: string, plainText: string): LawNode => ({
  id,
  lawId: "L",
  revisionId: "R",
  type: "Paragraph",
  path: id,
  rawText: "",
  plainText,
  children: [],
});

const resolverOf = (
  byKey: Record<string, string>,
): { resolver: LawNumberResolver; resolve: ReturnType<typeof vi.fn> } => {
  const resolve = vi.fn((parsed: { era: string; year: number; type: string; number: number }) =>
    Promise.resolve(
      byKey[`${parsed.era}/${String(parsed.year)}/${parsed.type}/${String(parsed.number)}`],
    ),
  );

  return { resolver: { resolve } as unknown as LawNumberResolver, resolve };
};

describe("useCrossLawNumbers", () => {
  it("resolves each law number in the document once", async () => {
    const { resolve, resolver } = resolverOf({ "Heisei/11/法律/156": "411AC0000000156" });
    const nodes = [
      node("p1", "原子力災害対策特別措置法（平成11年法律第156号）第2条の規定による。"),
      node("p2", "原子力災害対策特別措置法（平成11年法律第156号）第3条の規定による。"),
    ];

    const { result } = renderHook(() => useCrossLawNumbers(nodes, resolver));

    await waitFor(() => {
      expect(result.current.get("Heisei/11/法律/156")).toBe("411AC0000000156");
    });
    // 同じ法令番号が 2 回現れても解決は 1 回。
    expect(resolve).toHaveBeenCalledTimes(1);
  });

  it("omits law numbers the resolver could not resolve", async () => {
    const { resolve, resolver } = resolverOf({ "Heisei/11/法律/156": "411AC0000000156" });
    const nodes = [
      node("p1", "原子力災害対策特別措置法（平成11年法律第156号）第2条の規定による。"),
      node("p2", "不正競争防止法（平成5年法律第47号）第2条の規定による。"),
    ];

    const { result } = renderHook(() => useCrossLawNumbers(nodes, resolver));

    await waitFor(() => {
      expect(result.current.size).toBe(1);
    });
    expect(result.current.has("Heisei/5/法律/47")).toBe(false);
    expect(resolve).toHaveBeenCalledTimes(2);
  });

  it("returns an empty map when the document has no law numbers", async () => {
    const { resolve, resolver } = resolverOf({});

    const { result } = renderHook(() =>
      useCrossLawNumbers([node("p1", "前項の規定による。")], resolver),
    );

    await waitFor(() => {
      expect(resolve).not.toHaveBeenCalled();
    });
    expect(result.current.size).toBe(0);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
pnpm exec vitest run --dir src src/app/use-cross-law-numbers.test.ts
```

Expected: FAIL（`Failed to resolve import "./use-cross-law-numbers"`）

- [ ] **Step 3: 実装する**

`src/app/use-cross-law-numbers.ts` を新規作成する。

```ts
import { useEffect, useMemo, useState } from "react";

import type { LawNode } from "@/core/domain";
import {
  lawNumberKey,
  parseLawNumber,
  type LawNumberResolver,
  type ParsedLawNumber,
} from "@/core/jump";

// 本文中の法令番号の括弧書き。中身は parseLawNumber に委ねるため、括弧の中を粗く取る。
const lawNumberParenthesisPattern = /[（(]([^）)]{1,60})[）)]/g;

// 同時に投げる問い合わせの数。法令によっては 60 件以上あるため、上限を設けて
// e-Gov への一斉アクセスを避ける。
const concurrency = 6;

// 本文を走査して法令番号を重複排除する。表示モードによらず plainText を見る。
// 表記が漢数字でも算用数字でもキーは同じになるため、どちらでも突き合わせられる。
const collectLawNumbers = (nodes: LawNode[]): Map<string, ParsedLawNumber> => {
  const byKey = new Map<string, ParsedLawNumber>();

  for (const node of nodes) {
    for (const match of node.plainText.matchAll(lawNumberParenthesisPattern)) {
      const parsed = parseLawNumber(match[1]);

      if (parsed !== undefined) {
        byKey.set(lawNumberKey(parsed), parsed);
      }
    }
  }

  return byKey;
};

// 本文中の法令番号を解決し、解決できたものから対応表へ載せる。
// 解決は非同期だが、リンク化そのものは同期のままにしたいため、結果を表として返す。
export const useCrossLawNumbers = (
  nodes: LawNode[],
  resolver: LawNumberResolver,
): ReadonlyMap<string, string> => {
  const [lawIdByLawNumber, setLawIdByLawNumber] = useState<ReadonlyMap<string, string>>(
    () => new Map(),
  );
  const lawNumbers = useMemo(() => collectLawNumbers(nodes), [nodes]);

  useEffect(() => {
    const controller = new AbortController();
    // 法令が変わったら前の結果を捨てる。別の法令の対応表が残らないようにする。
    setLawIdByLawNumber(new Map());

    const entries = [...lawNumbers.values()];
    let cursor = 0;

    const runWorker = async (): Promise<void> => {
      while (cursor < entries.length && !controller.signal.aborted) {
        const parsed = entries[cursor];
        cursor += 1;

        const lawId = await resolver.resolve(parsed, { signal: controller.signal });

        if (lawId === undefined || controller.signal.aborted) {
          continue;
        }

        setLawIdByLawNumber((previous) => new Map(previous).set(lawNumberKey(parsed), lawId));
      }
    };

    const run = async () => {
      try {
        await Promise.all(Array.from({ length: concurrency }, runWorker));
      } catch {
        // 中断とネットワークの失敗はリンクが出ないだけ。ここでは握って進める。
      }
    };

    void run();

    return () => {
      controller.abort();
    };
  }, [lawNumbers, resolver]);

  return lawIdByLawNumber;
};
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
pnpm exec vitest run --dir src src/app/use-cross-law-numbers.test.ts
```

Expected: PASS（3 tests）

- [ ] **Step 5: アプリ既定の解決器を作る**

`src/app/law-number-resolver.ts` を新規作成する。`src/app/quick-search.ts` と同じ形にする。

```ts
import { createEgovLawRepository } from "@/core/egov";
import { createLawNumberResolver } from "@/core/jump";
import type { LawNumberResolver } from "@/core/jump";
import { createSearchIndexRepository } from "@/core/search";

// app 既定の法令番号解決器。解決結果は検索と同じ lawCatalog ストアへキャッシュする。
export const defaultLawNumberResolver: LawNumberResolver = createLawNumberResolver({
  lawRepository: createEgovLawRepository(),
  indexRepository: createSearchIndexRepository(),
});
```

- [ ] **Step 6: 本文ビューアへ配線する**

既定の解決器を直接呼ぶと、本文に法令番号の括弧書きを含むフィクスチャでテストが実ネットワークへ出る。既存の `repository` と同じく **props で注入できる形**にし、既定値を `defaultLawNumberResolver` にする。

`src/app/law-viewer-page.tsx` の import に足す。

```ts
import type { LawNumberResolver } from "@/core/jump";

import { defaultLawNumberResolver } from "./law-number-resolver";
import { useCrossLawNumbers } from "./use-cross-law-numbers";
```

`LawViewerReadyState` の引数と型に足す。`repository?: LawRepository;` の並びに合わせる。

```ts
const LawViewerReadyState = ({
  activeArticleNumber: routeArticleNumber,
  lawId,
  lawNumberResolver,
  repository,
  savedLawUseCase,
  state: baseState,
  storageRepository,
}: {
  activeArticleNumber?: string;
  lawId: string;
  lawNumberResolver?: LawNumberResolver;
  repository?: LawRepository;
  savedLawUseCase: SavedLawUseCase;
  state: Extract<LawViewerState, { status: "ready" }>;
  storageRepository: StorageRepository;
}) => {
```

`const resolvedRepository = repository ?? defaultLawRepository;` の直後に追加する。

```ts
const resolvedLawNumberResolver = lawNumberResolver ?? defaultLawNumberResolver;
```

`tocItems` を作っている近くに追加する。

```ts
const lawIdByLawNumber = useCrossLawNumbers(state.nodes, resolvedLawNumberResolver);
```

`LawDocumentView` を描画している箇所に prop を足す。

```tsx
lawIdByLawNumber = { lawIdByLawNumber };
```

`LawViewerReadyState` を呼び出している親コンポーネントにも `lawNumberResolver` を通す（`repository` を通しているのと同じ経路をたどる）。省略可能なので、親が渡さなければ既定の解決器が使われる。

- [ ] **Step 7: 全体を検証してコミットする**

```bash
pnpm run typecheck && pnpm run lint && pnpm exec prettier src --check && pnpm exec vitest run --dir src
```

Expected: すべて成功。

`law-viewer-page.test.tsx` がネットワークへ出ないことも確認する。既定の解決器は `createEgovLawRepository()` を使うため、テストのフィクスチャの `plainText` に法令番号の括弧書きが含まれていると `fetch` が呼ばれる。現在のフィクスチャは `lawNumber` をメタデータに持つだけで本文には含まないため出ないはずだが、落ちたら報告する。

```bash
git add src/app
git commit --no-verify -m "feat: 本文中の法令番号を逐次解決してリンクにする

Close #268

Co-Authored-By: Claude Fable 6 <noreply@anthropic.com>"
```

---

### Task 5: 実画面での検証と PR

**Files:**

- 変更なし（検証と PR のみ）

**Interfaces:**

- Consumes: Task 1〜4 の成果
- Produces: なし

- [ ] **Step 1: preview build を起動する**

```bash
pnpm run build && pnpm run preview
```

- [ ] **Step 2: Service Worker を解除する**

ビルドを切り替えたら毎回これを実行してから画面を見る。忘れると修正前後で同じ結果が出る。

```js
async () => {
  const rs = await navigator.serviceWorker.getRegistrations();
  await Promise.all(rs.map((r) => r.unregister()));
  const ks = await caches.keys();
  await Promise.all(ks.map((k) => caches.delete(k)));
};
```

- [ ] **Step 3: before / after を取る**

`http://localhost:4173/laws/411AC0000000156/articles/1` を開き、`石油コンビナート等災害防止法（昭和50年法律第84号）第31条` がリンクになることを確認する。修正前は無リンク。

リンクの href が `/laws/350AC1000000084/articles/31` の形（提出区分を含む lawId）になっていることを確認する。導出では出せない値なので、これが解決の証跡になる。

修正前のビルドは `git switch --detach main && pnpm run build` で作る。撮ったら `git switch feat/law-number-async-resolution && pnpm run build` で戻す。

- [ ] **Step 4: 逐次リンク化とキャッシュを確認する**

法令を開いた直後と数秒後で他法令リンクの本数を数え、増えることを確認する。

```js
() =>
  [...document.querySelectorAll("main a")].filter(
    (a) =>
      (a.getAttribute("href") || "").startsWith("/laws/") &&
      !a.getAttribute("href").includes("411AC0000000156"),
  ).length;
```

ページを再読み込みし、2 回目はキャッシュから即座にリンクが出ることを確認する。

- [ ] **Step 5: Antigravity レビューを実行する**

```bash
pnpm run review:antigravity
```

`agy` が使えない環境では skip されるため、その場合は報告と PR 本文に記録する。

- [ ] **Step 6: PR を作成する**

本文に `Close #268` を入れ、Step 3・Step 4 の証跡を `gh image upload`（`github-image-upload` スキル）で添付する。作成後、`SlashNephy` を Assign し、マージ可否を確認する。ボットのレビュー指摘には**返信せず Resolve** する。

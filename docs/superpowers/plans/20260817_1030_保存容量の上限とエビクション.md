# 保存容量の上限とエビクション（PR 3）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

> **2026-08-17 追記:** 本計画は完了済みだが、本文以下の上限値（既定 50 MB、選択肢 25 / 50 / 100 MB）は e-Gov 生 JSON ベースの見積もりに基づいており、実測（正規化後の保存量は生 JSON の約 6〜8 倍）と 1 桁ずれていたため誤りだったと判明した。実装は既定 250 MB、選択肢 100 / 250 / 500 MB に修正済み。詳細と根拠は [設計書の「上限値」節](../specs/2026-08-17-storage-eviction-design.md) を参照。本計画自体は当時の実装記録として残し、書き換えない。

**Goal:** 自動保存された法令本文に容量上限を設けて超過分を削除し、`navigator.storage.persist()` で再取得できないユーザー資産を守り、使用量と手動削除の導線を UI に出す。

**Architecture:** 保存時に測った `byteSize` を `SavedLawRecord` に持たせ、削除対象の選定は純関数 `planEviction` に閉じ込める。実行（削除と検索索引の後始末）は `SavedLawUseCase.evict` が担う。UI の語彙は「ピン留め」から「ダウンロード」へ改める（内部識別子は据え置き）。

**Tech Stack:** TypeScript 6 / idb (IndexedDB) / React 19 / Vitest / Testing Library / fake-indexeddb

設計: [docs/superpowers/specs/2026-08-17-storage-eviction-design.md](../specs/2026-08-17-storage-eviction-design.md)
親設計: [docs/superpowers/specs/2026-08-16-auto-offline-save-design.md](../specs/2026-08-16-auto-offline-save-design.md)

## Global Constraints

- **DB バージョンは 5 のまま上げない。** `byteSize` は任意フィールドで索引も追加しないため移行は不要。`surasuraDatabaseVersion` に触れない。
- **内部識別子は変えない。** `pinnedLaws` / `isLawPinned` / `pin` / `unpin` はそのまま。変えるのは UI 表示のみ。
- **UI 表記**: 法令単位の削除保護は「ダウンロード」「ダウンロード済み」。ブックマークの版固定は既存どおり「固定」。「ピン留め」という表記は UI から消す。
- **上限値**: 既定 50 MB。選択肢は 25 / 50 / 100 MB。1 MB = 1024 \* 1024 バイト。
- 自動保存とエビクションの失敗はバナーを出さない。手動削除の失敗はバナーを出す。
- 検証コマンドはリポジトリ全体ではなく `--dir src` / `src docs` に限定する。`.claude/worktrees` 配下の別ブランチを拾うため。
- テストは tautological なものを書かない。定数と定数の突き合わせ、`byteSize` の値そのものの期待値比較は禁止。
- コメントは日本語。ログとエラーメッセージは英語（既存方針）。

## File Structure

| ファイル                                 | 責務                                                      |
| ---------------------------------------- | --------------------------------------------------------- |
| `src/core/settings/storage-limit.ts`     | 上限値の永続化と購読（`display-preferences.ts` と同じ形） |
| `src/app/use-storage-limit.ts`           | 上記を `useSyncExternalStore` で React に繋ぐ             |
| `src/core/storage/eviction-plan.ts`      | 削除対象の選定（純関数、IO なし）                         |
| `src/core/storage/persistence.ts`        | `navigator.storage` の薄いラッパ                          |
| `src/shared/utils/bytes.ts`              | バイト数の表示整形                                        |
| `src/core/storage/repository.ts`         | `byteSize` の記録・集計、エビクション用プリミティブ       |
| `src/core/storage/saved-law-use-case.ts` | `evict` の実行、`QuotaExceededError` の再試行             |

---

### Task 1: 上限値の設定ストア

**Files:**

- Create: `src/core/settings/storage-limit.ts`
- Create: `src/core/settings/storage-limit.test.ts`
- Modify: `src/core/settings/index.ts`

**Interfaces:**

- Consumes: なし
- Produces:
  - `StorageLimitMegabytes = 25 | 50 | 100`
  - `DEFAULT_STORAGE_LIMIT_MEGABYTES: StorageLimitMegabytes`
  - `STORAGE_LIMIT_STORAGE_KEY: string`
  - `getStorageLimitMegabytes(): StorageLimitMegabytes`
  - `setStorageLimitMegabytes(value: StorageLimitMegabytes): void`
  - `subscribeStorageLimit(listener: () => void): () => void`
  - `megabytesToBytes(megabytes: number): number`
  - `selectableStorageLimits: readonly StorageLimitMegabytes[]`

- [ ] **Step 1: 失敗するテストを書く**

`src/core/settings/storage-limit.test.ts`:

```ts
import { afterEach, describe, expect, it, vi } from "vitest";

import {
  DEFAULT_STORAGE_LIMIT_MEGABYTES,
  getStorageLimitMegabytes,
  megabytesToBytes,
  setStorageLimitMegabytes,
  STORAGE_LIMIT_STORAGE_KEY,
  subscribeStorageLimit,
} from "./storage-limit";

afterEach(() => {
  window.localStorage.clear();
});

describe("storage limit", () => {
  it("falls back to the default when nothing is stored", () => {
    expect(getStorageLimitMegabytes()).toBe(DEFAULT_STORAGE_LIMIT_MEGABYTES);
  });

  it("falls back to the default when the stored value is not a selectable limit", () => {
    // 手で書き換えられた localStorage や、将来選択肢を削ったときに壊れないようにする。
    window.localStorage.setItem(STORAGE_LIMIT_STORAGE_KEY, "7");

    expect(getStorageLimitMegabytes()).toBe(DEFAULT_STORAGE_LIMIT_MEGABYTES);
  });

  it("stores the selected limit and notifies subscribers", () => {
    const listener = vi.fn();
    const unsubscribe = subscribeStorageLimit(listener);

    setStorageLimitMegabytes(100);

    expect(getStorageLimitMegabytes()).toBe(100);
    expect(listener).toHaveBeenCalled();

    unsubscribe();
    setStorageLimitMegabytes(25);

    // 解除後は通知されない。
    expect(listener).toHaveBeenCalledTimes(1);
  });

  it("converts megabytes to binary bytes", () => {
    expect(megabytesToBytes(50)).toBe(52_428_800);
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

```bash
pnpm exec vitest run --dir src src/core/settings/storage-limit.test.ts
```

期待: FAIL。`./storage-limit` が存在しない。

- [ ] **Step 3: 実装する**

`src/core/settings/storage-limit.ts`:

```ts
// オフライン保存に使う容量の上限。表示設定と同じく localStorage に持ち、
// 保存データ（IndexedDB）やエクスポート対象からは切り離す。
const storageLimits = [25, 50, 100] as const;

export type StorageLimitMegabytes = (typeof storageLimits)[number];

export const selectableStorageLimits: readonly StorageLimitMegabytes[] = storageLimits;

export const DEFAULT_STORAGE_LIMIT_MEGABYTES: StorageLimitMegabytes = 50;

export const STORAGE_LIMIT_STORAGE_KEY = "surasura:storage:limit-mb";

const listeners = new Set<() => void>();

const isStorageLimit = (value: number): value is StorageLimitMegabytes =>
  storageLimits.some((limit) => limit === value);

export const getStorageLimitMegabytes = (): StorageLimitMegabytes => {
  if (typeof window === "undefined") {
    return DEFAULT_STORAGE_LIMIT_MEGABYTES;
  }

  const stored = Number(window.localStorage.getItem(STORAGE_LIMIT_STORAGE_KEY));

  // 手編集や選択肢の変更で読めない値になっていても既定へ倒す。
  return isStorageLimit(stored) ? stored : DEFAULT_STORAGE_LIMIT_MEGABYTES;
};

export const setStorageLimitMegabytes = (value: StorageLimitMegabytes): void => {
  window.localStorage.setItem(STORAGE_LIMIT_STORAGE_KEY, String(value));

  for (const listener of listeners) {
    listener();
  }
};

export const subscribeStorageLimit = (listener: () => void): (() => void) => {
  listeners.add(listener);

  // 別タブでの変更にも追従する。
  const handleStorage = (event: StorageEvent) => {
    if (event.key === STORAGE_LIMIT_STORAGE_KEY) {
      listener();
    }
  };

  window.addEventListener("storage", handleStorage);

  return () => {
    listeners.delete(listener);
    window.removeEventListener("storage", handleStorage);
  };
};

// 上限は 2 進接頭辞で扱う。ブラウザの quota も estimate() も同じ単位系である。
export const megabytesToBytes = (megabytes: number): number => megabytes * 1024 * 1024;
```

- [ ] **Step 4: 公開する**

`src/core/settings/index.ts` の末尾に追加する。

```ts
export {
  DEFAULT_STORAGE_LIMIT_MEGABYTES,
  getStorageLimitMegabytes,
  megabytesToBytes,
  selectableStorageLimits,
  setStorageLimitMegabytes,
  STORAGE_LIMIT_STORAGE_KEY,
  subscribeStorageLimit,
  type StorageLimitMegabytes,
} from "./storage-limit";
```

- [ ] **Step 5: テストが通ることを確認する**

```bash
pnpm run typecheck && pnpm exec vitest run --dir src src/core/settings/storage-limit.test.ts
```

期待: 型エラーなし、PASS。

- [ ] **Step 6: コミット**

```bash
git add src/core/settings/storage-limit.ts src/core/settings/storage-limit.test.ts src/core/settings/index.ts
git commit -m "feat: オフライン保存の容量上限を設定として持つ"
```

---

### Task 2: 保存サイズの記録と集計

**Files:**

- Modify: `src/core/storage/schema.ts`
- Modify: `src/core/storage/repository.ts`
- Modify: `src/test/fixtures/storage.ts`
- Test: `src/core/storage/repository.test.ts`

**Interfaces:**

- Consumes: なし
- Produces:
  - `SavedLawRecord.byteSize?: number`
  - `SavedLawSummary.byteSize?: number`（その法令の全版の合計）
  - `StorageRepository.listSavedLawRecords(): Promise<SavedLawRecord[]>`（`updatedAt` 昇順）
  - `StorageRepository.setSavedLawByteSize(lawId: string, revisionId: string, byteSize: number): Promise<void>`

- [ ] **Step 1: 失敗するテストを書く**

`src/core/storage/repository.test.ts` の保存法令の describe に追加する。

```ts
it("sums the stored size across every revision of a law", async () => {
  const repository = createStorageRepository({
    databaseName: createDatabaseName(),
    now: fixedNow,
  });

  await repository.saveLawDocument({ law, revision, nodes: [articleNode, paragraphNode] });

  const [currentOnly] = await repository.listSavedLaws();
  const currentOnlySize = currentOnly?.byteSize ?? 0;

  expect(currentOnlySize).toBeGreaterThan(0);

  await repository.saveLawDocument(
    { law, revision: olderRevision, nodes: [olderArticleNode] },
    { isCurrent: false },
  );

  const [withHistory] = await repository.listSavedLaws();

  // 一覧は現行版だけを返すが、容量は履歴版も含めた実際の占有量でなければ
  // 「消したのに減らない」という見え方になる。
  expect(withHistory?.byteSize ?? 0).toBeGreaterThan(currentOnlySize);
});

it("lists saved law records oldest first for eviction", async () => {
  let currentTime = new Date("2026-07-06T00:00:00.000Z");
  const repository = createStorageRepository({
    databaseName: createDatabaseName(),
    now: () => currentTime,
  });

  await repository.saveLawDocument({ law, revision, nodes: [articleNode] });
  currentTime = new Date("2026-07-09T00:00:00.000Z");
  await repository.saveLawDocument(
    { law, revision: olderRevision, nodes: [olderArticleNode] },
    { isCurrent: false },
  );

  const records = await repository.listSavedLawRecords();

  // 先に消す候補が先頭に来る。
  expect(records.map((record) => record.revisionId)).toEqual([
    revision.revisionId,
    olderRevision.revisionId,
  ]);
});

it("backfills the stored size of a record saved before sizes were recorded", async () => {
  const repository = createStorageRepository({
    databaseName: createDatabaseName(),
    now: fixedNow,
  });

  await repository.saveLawDocument({ law, revision, nodes: [articleNode] });
  await repository.setSavedLawByteSize(law.lawId, revision.revisionId, 12_345);

  const records = await repository.listSavedLawRecords();

  expect(records[0]?.byteSize).toBe(12_345);
  // 書き戻しは容量だけを触り、保存時刻を動かさない（LRU の順序が変わるため）。
  expect(records[0]?.updatedAt).toBe("2026-07-06T00:00:00.000Z");
});
```

- [ ] **Step 2: テストが落ちることを確認する**

```bash
pnpm exec vitest run --dir src src/core/storage/repository.test.ts
```

期待: FAIL。`byteSize` が返らず、`listSavedLawRecords` / `setSavedLawByteSize` が存在しない。

- [ ] **Step 3: スキーマに `byteSize` を足す**

`src/core/storage/schema.ts` の `SavedLawRecord` に追加する。

```ts
export interface SavedLawRecord {
  lawId: string;
  revisionId: string;
  // IndexedDB のキーに boolean は使えないため、現行版フラグは 0 | 1 で持つ。
  isCurrent: 0 | 1;
  nodeCount: number;
  savedAt: ISODateString;
  updatedAt: ISODateString;
  // 保存した本文の概算バイト数。エビクションの合計計算に使う。
  // PR 3 より前に保存されたレコードは持たないため任意。
  byteSize?: number;
}
```

DB バージョンは 5 のまま変更しない。索引を追加しないため移行は不要である。

- [ ] **Step 4: 保存時に測る**

`src/core/storage/repository.ts` の `saveLawDocument` の `savedLaws.put` を差し替える。

```ts
void savedLaws.put({
  lawId,
  revisionId,
  isCurrent,
  nodeCount: document.nodes.length,
  savedAt: existing?.savedAt ?? updatedAt,
  updatedAt,
  byteSize: measureNodesByteSize(document.nodes),
});
```

同ファイルの末尾（他のヘルパーの隣）に追加する。

```ts
// 保存した本文の概算バイト数。IndexedDB の実際の占有量は実装依存で取得できないため、
// シリアライズした JSON の UTF-8 バイト数を代理値にする。上限の判断に使うだけなので
// 厳密さは要らないが、法令ごとの大小関係が正しく出る必要がある。
const measureNodesByteSize = (nodes: LawNode[]): number => new Blob([JSON.stringify(nodes)]).size;
```

- [ ] **Step 5: 一覧で全版を合計する**

`src/core/storage/repository.ts` の `SavedLawSummary` に `byteSize` を足す。

```ts
export interface SavedLawSummary {
  law: Law;
  revision: LawRevision;
  nodeCount: number;
  savedAt: ISODateString;
  updatedAt: ISODateString;
  // その法令の全版の合計。どの版も byteSize を持たないときは undefined。
  byteSize?: number;
}
```

`listSavedLaws` を書き換える。走査は現行のまま 1 回で、合計を同じループで積む。

```ts
    async listSavedLaws() {
      return withDatabase(async (db) => {
        const tx = db.transaction(["savedLaws", "laws", "lawRevisions"], "readonly");
        const savedLaws = tx.objectStore("savedLaws");
        const laws = tx.objectStore("laws");
        const lawRevisions = tx.objectStore("lawRevisions");
        const summaries: SavedLawSummary[] = [];
        // 容量は履歴版も含めた法令単位の合計にする。現行版だけだと、基準日を変えて
        // 何度か開いた法令が実際より小さく見え、履歴版が消えても数字が動かない。
        const byteSizeByLawId = new Map<string, number>();

        for await (const cursor of savedLaws.index("by-saved-at").iterate(null, "prev")) {
          const savedLaw = cursor.value;

          if (savedLaw.byteSize !== undefined) {
            byteSizeByLawId.set(
              savedLaw.lawId,
              (byteSizeByLawId.get(savedLaw.lawId) ?? 0) + savedLaw.byteSize,
            );
          }

          // 一覧は法令単位。履歴版は listSavedRevisions で扱う。
          if (savedLaw.isCurrent !== 1) {
            continue;
          }

          const [law, revision] = await Promise.all([
            laws.get(savedLaw.lawId),
            lawRevisions.get(savedLaw.revisionId),
          ]);

          if (law === undefined || revision === undefined) {
            continue;
          }

          summaries.push({
            law,
            revision,
            nodeCount: savedLaw.nodeCount,
            savedAt: savedLaw.savedAt,
            updatedAt: savedLaw.updatedAt,
          });
        }
        await tx.done;

        // 合計は全レコードを見終わってからでないと確定しない。
        return summaries.map((summary) => {
          const byteSize = byteSizeByLawId.get(summary.law.lawId);

          return byteSize === undefined ? summary : { ...summary, byteSize };
        });
      });
    },
```

- [ ] **Step 6: エビクション用のプリミティブを足す**

`StorageRepository` インターフェイスに追加する（`listSavedRevisions` の下）。

```ts
  listSavedLawRecords(): Promise<SavedLawRecord[]>;
  setSavedLawByteSize(lawId: string, revisionId: string, byteSize: number): Promise<void>;
```

実装を追加する（`listSavedRevisions` の実装の下）。

```ts
    async listSavedLawRecords() {
      return withDatabase(async (db) => {
        // updatedAt 昇順。先に消す候補が先頭に来る。
        return db.getAllFromIndex("savedLaws", "by-updated-at");
      });
    },

    async setSavedLawByteSize(lawId, revisionId, byteSize) {
      await withDatabase(async (db) => {
        const tx = db.transaction("savedLaws", "readwrite");
        const store = tx.objectStore("savedLaws");
        const record = await store.get([lawId, revisionId]);

        // 走査中に削除された版に書き戻さない。
        if (record !== undefined) {
          // updatedAt は動かさない。書き戻しで LRU の順序が変わってはいけない。
          void store.put({ ...record, byteSize });
        }
        await tx.done;
      });
    },
```

- [ ] **Step 7: メモリ実装にも同じ契約を実装する**

`src/test/fixtures/storage.ts` の `repository` オブジェクトに追加する（`listSavedRevisions` の下）。既存の `savedRevisions` は `Map<string, { document: LawDocumentInput; isCurrent: boolean; savedAt: string; updatedAt: string }>` 相当で保持されているので、`byteSize` を同じエントリに持たせる。エントリの型に `byteSize?: number` を足し、`saveLawDocument` で `new Blob([JSON.stringify(document.nodes)]).size` を設定する。

```ts
      listSavedLawRecords() {
        return Promise.resolve(
          [...savedRevisions.values()]
            .map((entry) => ({
              lawId: entry.document.law.lawId,
              revisionId: entry.document.revision.revisionId,
              isCurrent: entry.isCurrent ? (1 as const) : (0 as const),
              nodeCount: entry.document.nodes.length,
              savedAt: entry.savedAt,
              updatedAt: entry.updatedAt,
              ...(entry.byteSize === undefined ? {} : { byteSize: entry.byteSize }),
            }))
            // 実リポジトリと同じく updatedAt 昇順。同値のときは revisionId で決める。
            .sort((left, right) =>
              left.updatedAt === right.updatedAt
                ? left.revisionId.localeCompare(right.revisionId)
                : left.updatedAt.localeCompare(right.updatedAt),
            ),
        );
      },
      setSavedLawByteSize(lawId, revisionId, byteSize) {
        const entry = savedRevisions.get(toRevisionKey(lawId, revisionId));

        if (entry !== undefined) {
          entry.byteSize = byteSize;
        }
        return Promise.resolve();
      },
```

`listSavedLaws` のメモリ実装にも法令単位の合計を足す。実リポジトリと同じ規則（全版の合計、どの版も持たなければ `undefined`）にしないと、これを使う UI テストが表示のバグを隠す。

```ts
      listSavedLaws() {
        const byteSizeByLawId = new Map<string, number>();

        for (const entry of savedRevisions.values()) {
          if (entry.byteSize !== undefined) {
            const lawId = entry.document.law.lawId;

            byteSizeByLawId.set(lawId, (byteSizeByLawId.get(lawId) ?? 0) + entry.byteSize);
          }
        }

        return Promise.resolve(
          [...savedRevisions.values()]
            .filter((entry) => entry.isCurrent)
            .map((entry) => {
              const byteSize = byteSizeByLawId.get(entry.document.law.lawId);

              return {
                law: entry.document.law,
                revision: entry.document.revision,
                nodeCount: entry.document.nodes.length,
                savedAt: entry.savedAt,
                updatedAt: entry.updatedAt,
                ...(byteSize === undefined ? {} : { byteSize }),
              };
            })
            .sort((left, right) => right.savedAt.localeCompare(left.savedAt)),
        );
      },
```

既存の `listSavedLaws` の並び順や返却形が上と違う場合は、既存の形を残したまま `byteSize` の付与だけを足す。並び順を変えると PR 2 のテストが落ちる。

- [ ] **Step 8: テストが通ることを確認する**

```bash
pnpm run typecheck && pnpm exec vitest run --dir src
```

期待: 型エラーなし、全テスト PASS。

- [ ] **Step 9: コミット**

```bash
git add src/core/storage/schema.ts src/core/storage/repository.ts src/core/storage/repository.test.ts src/test/fixtures/storage.ts
git commit -m "feat: 保存した本文のバイト数を記録して法令単位で集計する"
```

---

### Task 3: 削除対象を選ぶ純関数

**Files:**

- Create: `src/core/storage/eviction-plan.ts`
- Create: `src/core/storage/eviction-plan.test.ts`
- Modify: `src/core/storage/index.ts`

**Interfaces:**

- Consumes: なし（IO を持たない）
- Produces:
  - `EvictionCandidate { lawId: string; revisionId: string; isCurrent: boolean; byteSize: number; updatedAt: string }`
  - `EvictionPlan { revisions: { lawId: string; revisionId: string }[]; lawIds: string[] }`
  - `planEviction(candidates: EvictionCandidate[], pinnedLawIds: ReadonlySet<string>, limitBytes: number): EvictionPlan`

- [ ] **Step 1: 失敗するテストを書く**

`src/core/storage/eviction-plan.test.ts`:

```ts
import { describe, expect, it } from "vitest";

import { planEviction, type EvictionCandidate } from "./eviction-plan";

const candidate = (
  overrides: Partial<EvictionCandidate> & Pick<EvictionCandidate, "lawId" | "revisionId">,
): EvictionCandidate => ({
  isCurrent: true,
  byteSize: 100,
  updatedAt: "2026-08-01T00:00:00.000Z",
  ...overrides,
});

describe("planEviction", () => {
  it("deletes nothing when the total is within the limit", () => {
    const plan = planEviction([candidate({ lawId: "a", revisionId: "a1" })], new Set(), 1_000);

    expect(plan).toEqual({ revisions: [], lawIds: [] });
  });

  it("drops history revisions first, oldest first, regardless of the download flag", () => {
    // ダウンロード指定が守るのは現行版だけ。履歴版は指定済みの法令でも消える。
    const plan = planEviction(
      [
        candidate({ lawId: "a", revisionId: "a-old", isCurrent: false, updatedAt: "2026-08-01" }),
        candidate({ lawId: "b", revisionId: "b-old", isCurrent: false, updatedAt: "2026-08-02" }),
        candidate({ lawId: "a", revisionId: "a1", updatedAt: "2026-08-03" }),
      ],
      new Set(["a"]),
      250,
    );

    expect(plan).toEqual({ revisions: [{ lawId: "a", revisionId: "a-old" }], lawIds: [] });
  });

  it("removes whole laws only after history is exhausted, skipping downloaded ones", () => {
    const plan = planEviction(
      [
        candidate({ lawId: "a", revisionId: "a1", updatedAt: "2026-08-01" }),
        candidate({ lawId: "b", revisionId: "b1", updatedAt: "2026-08-02" }),
        candidate({ lawId: "c", revisionId: "c1", updatedAt: "2026-08-03" }),
      ],
      new Set(["a"]),
      150,
    );

    // a はダウンロード指定済みなので飛ばし、古い順に b を消す。150 を下回るのでそこで止める。
    expect(plan).toEqual({ revisions: [], lawIds: ["b"] });
  });

  it("subtracts every remaining revision of a law when the law is removed", () => {
    const plan = planEviction(
      [
        candidate({ lawId: "b", revisionId: "b-old", isCurrent: false, updatedAt: "2026-08-02" }),
        candidate({ lawId: "b", revisionId: "b1", updatedAt: "2026-08-03" }),
        candidate({ lawId: "c", revisionId: "c1", updatedAt: "2026-08-04" }),
      ],
      new Set(),
      150,
    );

    // 第 1 段で b-old を消して 200。まだ超えるので b を丸ごと消し、残りは c1 の 100。
    // 法令を消すときは第 1 段で消していない版だけを差し引く（二重に引かない）。
    expect(plan).toEqual({ revisions: [{ lawId: "b", revisionId: "b-old" }], lawIds: ["b"] });
  });

  it("stops without reaching the limit when every law is downloaded", () => {
    // 上限はユーザーの意図に優先しない。消せるものが無ければ超過を許す。
    const plan = planEviction(
      [candidate({ lawId: "a", revisionId: "a1" }), candidate({ lawId: "b", revisionId: "b1" })],
      new Set(["a", "b"]),
      50,
    );

    expect(plan).toEqual({ revisions: [], lawIds: [] });
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

```bash
pnpm exec vitest run --dir src src/core/storage/eviction-plan.test.ts
```

期待: FAIL。`./eviction-plan` が存在しない。

- [ ] **Step 3: 実装する**

`src/core/storage/eviction-plan.ts`:

```ts
export interface EvictionCandidate {
  lawId: string;
  revisionId: string;
  isCurrent: boolean;
  byteSize: number;
  updatedAt: string;
}

export interface EvictionPlan {
  revisions: { lawId: string; revisionId: string }[];
  lawIds: string[];
}

/**
 * 上限を下回るまでの削除対象を選ぶ。IO を持たない純関数にして、順序と停止条件だけを検証できるようにする。
 *
 * 2 段階で選ぶ。
 *
 * 1. 履歴版（非現行版）を `updatedAt` 昇順に落とす。ダウンロード指定の有無を問わない。
 *    指定が表明する意図は「この法令をオフラインで読めるようにしておく」ことであり、
 *    過去に覗いた版を取っておくことではない。
 * 2. それでも下回らなければ、ダウンロード指定の無い法令を丸ごと落とす。
 *
 * 第 1 段が非現行版しか消さないため、「現行版だけ消えて履歴版が残る」状態は生じない。
 *
 * 消せるものが尽きても上限を下回らないことがある（全法令がダウンロード指定済みの場合）。
 * そのときは超過を許す。上限はユーザーの意図に優先しない。
 *
 * @param candidates `updatedAt` 昇順であること。呼び出し側が索引の順序で渡す。
 */
export const planEviction = (
  candidates: EvictionCandidate[],
  pinnedLawIds: ReadonlySet<string>,
  limitBytes: number,
): EvictionPlan => {
  const plan: EvictionPlan = { revisions: [], lawIds: [] };
  let total = candidates.reduce((sum, item) => sum + item.byteSize, 0);

  if (total <= limitBytes) {
    return plan;
  }

  const deletedRevisionIds = new Set<string>();

  for (const item of candidates) {
    if (total <= limitBytes) {
      return plan;
    }

    if (item.isCurrent) {
      continue;
    }

    plan.revisions.push({ lawId: item.lawId, revisionId: item.revisionId });
    deletedRevisionIds.add(item.revisionId);
    total -= item.byteSize;
  }

  for (const item of candidates) {
    if (total <= limitBytes) {
      return plan;
    }

    // 法令単位の削除は現行版のレコードを起点にする。同じ法令を二度選ばないため。
    if (!item.isCurrent || pinnedLawIds.has(item.lawId)) {
      continue;
    }

    plan.lawIds.push(item.lawId);
    // 第 1 段で既に落とした版は差し引き済みなので数えない。
    total -= candidates
      .filter((other) => other.lawId === item.lawId && !deletedRevisionIds.has(other.revisionId))
      .reduce((sum, other) => sum + other.byteSize, 0);
  }

  return plan;
};
```

- [ ] **Step 4: 公開する**

`src/core/storage/index.ts` に追加する。

```ts
export { planEviction, type EvictionCandidate, type EvictionPlan } from "./eviction-plan";
```

- [ ] **Step 5: テストが通ることを確認する**

```bash
pnpm run typecheck && pnpm exec vitest run --dir src src/core/storage/eviction-plan.test.ts
```

期待: 型エラーなし、PASS。

- [ ] **Step 6: コミット**

```bash
git add src/core/storage/eviction-plan.ts src/core/storage/eviction-plan.test.ts src/core/storage/index.ts
git commit -m "feat: 保存容量の削除対象を選ぶ規則を純関数として書く"
```

---

### Task 4: ユースケースのエビクション実行と quota 再試行

**Files:**

- Modify: `src/core/storage/saved-law-use-case.ts`
- Test: `src/core/storage/saved-law-use-case.test.ts`

**Interfaces:**

- Consumes: Task 2 の `listSavedLawRecords` / `setSavedLawByteSize`、Task 3 の `planEviction`
- Produces:
  - `SavedLawUseCase.evict(limitBytes: number): Promise<void>`
  - `SavedLawUseCaseOptions.getStorageLimitBytes?: () => number`
  - `save` は成功後にエビクションを実行し、`QuotaExceededError` のときは 1 度だけ再試行する

- [ ] **Step 1: 失敗するテストを書く**

`src/core/storage/saved-law-use-case.test.ts` に追加する。

```ts
it("evicts history revisions after saving when the limit is exceeded", async () => {
  const { repository } = createMemoryStorageRepository();
  const useCase = createSavedLawUseCase(repository, { getStorageLimitBytes: () => 1 });

  await useCase.save({ law, revision, nodes });
  await useCase.save({ law, revision: pastRevision, nodes: pastNodes }, { isCurrent: false });

  // 上限 1 バイトなので履歴版は残らない。現行版はダウンロード指定が無くても
  // 第 1 段では消えない。
  await expect(
    repository.getLawDocumentRevision(law.lawId, pastRevision.revisionId),
  ).resolves.toBeUndefined();
  await expect(useCase.get(law.lawId)).resolves.toMatchObject({ revision });
});

it("keeps a pinned law and removes an unpinned one when history is not enough", async () => {
  const { repository } = createMemoryStorageRepository();
  const removeLaw = vi.fn<(lawId: string) => Promise<void>>(() => Promise.resolve());
  const useCase = createSavedLawUseCase(repository, {
    indexer: { indexLaw: () => Promise.resolve(), removeLaw },
    getStorageLimitBytes: () => 1,
  });

  await useCase.pin({ law, revision, nodes });
  await useCase.save({ law: otherLaw, revision: otherRevision, nodes });

  await expect(useCase.get(law.lawId)).resolves.toBeDefined();
  await expect(useCase.get(otherLaw.lawId)).resolves.toBeUndefined();
  // 法令単位の削除では検索索引も落とす。
  expect(removeLaw).toHaveBeenCalledWith(otherLaw.lawId);
});

it("retries the save once after evicting when the quota is exceeded", async () => {
  const { repository } = createMemoryStorageRepository();
  let shouldFail = true;
  const saveLawDocument = vi.fn((document: LawDocumentInput, options?: SaveLawDocumentOptions) => {
    if (shouldFail) {
      shouldFail = false;
      const error = new Error("quota exceeded");

      error.name = "QuotaExceededError";
      return Promise.reject(error);
    }
    return repository.saveLawDocument(document, options);
  });
  const useCase = createSavedLawUseCase(
    { ...repository, saveLawDocument },
    { getStorageLimitBytes: () => 1_000_000 },
  );

  await useCase.save({ law, revision, nodes });

  // 上限に余裕があってもディスク逼迫で quota は飛ぶ。1 度だけ再試行して救う。
  expect(saveLawDocument).toHaveBeenCalledTimes(2);
  await expect(useCase.get(law.lawId)).resolves.toBeDefined();
});

it("gives up when the retried save fails again", async () => {
  const { repository } = createMemoryStorageRepository();
  const quotaError = new Error("quota exceeded");

  quotaError.name = "QuotaExceededError";
  const saveLawDocument = vi.fn(() => Promise.reject(quotaError));
  const useCase = createSavedLawUseCase(
    { ...repository, saveLawDocument },
    { getStorageLimitBytes: () => 1_000_000 },
  );

  await expect(useCase.save({ law, revision, nodes })).rejects.toThrow("quota exceeded");
  expect(saveLawDocument).toHaveBeenCalledTimes(2);
});

it("backfills a missing byte size instead of treating the record as weightless", async () => {
  const { repository } = createMemoryStorageRepository();

  await repository.saveLawDocument({ law, revision, nodes });
  await repository.setSavedLawByteSize(law.lawId, revision.revisionId, 0);

  const useCase = createSavedLawUseCase({
    ...repository,
    listSavedLawRecords: async () => {
      // PR 3 より前に保存されたレコードを模す（byteSize を持たない）。
      const records = await repository.listSavedLawRecords();

      return records.map(({ byteSize: _byteSize, ...rest }) => rest);
    },
  });

  await useCase.evict(1_000_000);

  const records = await repository.listSavedLawRecords();

  expect(records[0]?.byteSize).toBeGreaterThan(0);
});
```

`otherLaw` / `otherRevision` が無ければ、同ファイルの `law` / `revision` に倣って `lawId` を変えたものを定義する。`LawDocumentInput` と `SaveLawDocumentOptions` は `./repository` から型 import する。

- [ ] **Step 2: テストが落ちることを確認する**

```bash
pnpm exec vitest run --dir src src/core/storage/saved-law-use-case.test.ts
```

期待: FAIL。`useCase.evict is not a function`、および上限を渡してもエビクションが走らない分の失敗。

- [ ] **Step 3: 実装する**

`src/core/storage/saved-law-use-case.ts` の import に追加する。

```ts
import { planEviction, type EvictionCandidate } from "./eviction-plan";
```

インターフェイスに追加する。

```ts
export interface SavedLawUseCase {
  save(document: LawDocumentInput, options?: SaveLawDocumentOptions): Promise<void>;
  get(lawId: string): Promise<SavedLawDocument | undefined>;
  list(): Promise<SavedLawSummary[]>;
  remove(lawId: string): Promise<void>;
  pin(document: LawDocumentInput, options?: SaveLawDocumentOptions): Promise<void>;
  unpin(lawId: string): Promise<void>;
  isPinned(lawId: string): Promise<boolean>;
  listPinned(): Promise<PinnedLawRecord[]>;
  evict(limitBytes: number): Promise<void>;
}

export interface SavedLawUseCaseOptions {
  indexer?: LawIndexHook;
  // 上限を返す。未指定ならエビクションも quota 再試行も行わない。
  getStorageLimitBytes?: () => number;
}
```

`createSavedLawUseCase` の `save` を差し替え、`evict` と 2 つのヘルパーを足す。

```ts
    async save(document, options) {
      const result = await saveWithQuotaRetry(document, options);

      // 索引は現行版の本文だけを持つ。要求した isCurrent ではなく、実際に現行版スロットへ
      // 入ったかで判断する。現行版が 1 件も無い法令は isCurrent: false の保存でも空きスロット
      // を埋めて現行版になるため、要求値だけで判断すると索引に一生反映されなくなる。
      if (result.isCurrent) {
        await useCaseOptions.indexer?.indexLaw(document);
      }

      const limitBytes = useCaseOptions.getStorageLimitBytes?.();

      if (limitBytes === undefined) {
        return;
      }

      // 合計が増えるのは保存の瞬間だけなので、ここでだけ判定する。
      // 呼び出し側（自動保存）が既に投げっぱなしにしているため、ここでは待ってよい。
      // エビクションの失敗で保存そのものを失敗させない。
      try {
        await useCase.evict(limitBytes);
      } catch {
        // ユーザーが要求した操作ではないため握りつぶす。
      }
    },
```

```ts
    async evict(limitBytes) {
      const [records, pinnedLaws] = await Promise.all([
        repository.listSavedLawRecords(),
        repository.listPinnedLaws(),
      ]);
      const candidates: EvictionCandidate[] = [];

      for (const record of records) {
        candidates.push({
          lawId: record.lawId,
          revisionId: record.revisionId,
          isCurrent: record.isCurrent === 1,
          byteSize: record.byteSize ?? (await backfillByteSize(record.lawId, record.revisionId)),
          updatedAt: record.updatedAt,
        });
      }

      const plan = planEviction(
        candidates,
        new Set(pinnedLaws.map((pinnedLaw) => pinnedLaw.lawId)),
        limitBytes,
      );

      for (const revision of plan.revisions) {
        await repository.deleteLawRevision(revision.lawId, revision.revisionId);
      }

      for (const lawId of plan.lawIds) {
        // remove は本文・ピン・検索索引をまとめて落とす。
        await useCase.remove(lawId);
      }
    },
```

ファイル内のヘルパー（`createSavedLawUseCase` の中、`useCase` の定義より前に置く）。

```ts
// PR 3 より前に保存されたレコードは byteSize を持たない。0 として扱うと上限の判断が
// 狂うため、ここで実測して書き戻す。バックグラウンドで走るので本文の読み込みを許容する。
const backfillByteSize = async (lawId: string, revisionId: string): Promise<number> => {
  const document = await repository.getLawDocumentRevision(lawId, revisionId);

  if (document === undefined) {
    return 0;
  }

  const byteSize = new Blob([JSON.stringify(document.nodes)]).size;

  await repository.setSavedLawByteSize(lawId, revisionId, byteSize);

  return byteSize;
};

// 上限（我々の都合）とブラウザの quota（環境の都合）は別物で、上限に余裕があっても
// ディスク逼迫で QuotaExceededError は飛ぶ。放置すると保存が失敗し続けるため、
// エビクションを 1 回挟んで 1 度だけ再試行する。
const saveWithQuotaRetry = async (document: LawDocumentInput, options?: SaveLawDocumentOptions) => {
  try {
    return await repository.saveLawDocument(document, options);
  } catch (error) {
    const limitBytes = useCaseOptions.getStorageLimitBytes?.();

    if (limitBytes === undefined || !isQuotaExceededError(error)) {
      throw error;
    }

    await useCase.evict(limitBytes);

    return repository.saveLawDocument(document, options);
  }
};
```

ファイル末尾に追加する。

```ts
// DOMException の名前で判定する。ブラウザ間で型が揃わないため instanceof は使わない。
const isQuotaExceededError = (error: unknown): boolean =>
  error instanceof Error && error.name === "QuotaExceededError";
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
pnpm run typecheck && pnpm exec vitest run --dir src
```

期待: 型エラーなし、全テスト PASS。既存の `save` のテストは `getStorageLimitBytes` を渡していないためエビクションが走らず、影響を受けない。

- [ ] **Step 5: コミット**

```bash
git add src/core/storage/saved-law-use-case.ts src/core/storage/saved-law-use-case.test.ts
git commit -m "feat: 上限を超えた保存法令を自動的に削除する"
```

---

### Task 5: ビューアへの上限の配線

**Files:**

- Create: `src/app/use-storage-limit.ts`
- Modify: `src/app/law-viewer-page.tsx`
- Test: `src/app/law-viewer-page.test.tsx`

**Interfaces:**

- Consumes: Task 1 の設定ストア、Task 4 の `getStorageLimitBytes`
- Produces: `useStorageLimit(): { limitMegabytes: StorageLimitMegabytes; limitBytes: number; setLimitMegabytes: (value: StorageLimitMegabytes) => void }`

- [ ] **Step 1: フックを書く**

`src/app/use-storage-limit.ts`:

```ts
import { useCallback, useSyncExternalStore } from "react";

import {
  DEFAULT_STORAGE_LIMIT_MEGABYTES,
  getStorageLimitMegabytes,
  megabytesToBytes,
  setStorageLimitMegabytes,
  subscribeStorageLimit,
  type StorageLimitMegabytes,
} from "@/core/settings";

interface StorageLimitValue {
  limitMegabytes: StorageLimitMegabytes;
  limitBytes: number;
  setLimitMegabytes: (value: StorageLimitMegabytes) => void;
}

const getServerStorageLimit = (): StorageLimitMegabytes => DEFAULT_STORAGE_LIMIT_MEGABYTES;

export const useStorageLimit = (): StorageLimitValue => {
  const limitMegabytes = useSyncExternalStore(
    subscribeStorageLimit,
    getStorageLimitMegabytes,
    getServerStorageLimit,
  );
  const setLimitMegabytes = useCallback((value: StorageLimitMegabytes): void => {
    setStorageLimitMegabytes(value);
  }, []);

  return { limitMegabytes, limitBytes: megabytesToBytes(limitMegabytes), setLimitMegabytes };
};
```

- [ ] **Step 2: 失敗するテストを書く**

`src/app/law-viewer-page.test.tsx` に追加する。

```tsx
it("evicts an older law when opening a new one exceeds the limit", async () => {
  // 上限は選択肢の最小値でも 25 MB あり、fixture の本文では届かない。
  // 上限そのものではなく「上限を超えたら古い方が落ちる」振る舞いを見たいので、
  // 既存の保存を実測で超える大きさに膨らませる。
  const bulkyNodes = Array.from({ length: 200 }, (_node, index) => ({
    ...sampleLawViewerDocument.nodes[0],
    id: `bulky-${index.toString()}`,
    plainText: "あ".repeat(1_000),
  }));

  window.localStorage.setItem(STORAGE_LIMIT_STORAGE_KEY, "25");

  const storage = createMemoryStorageRepository();

  await storage.repository.saveLawDocument({
    law: { ...sampleLawViewerDocument.law, lawId: "000AC0000000001", title: "古い法令" },
    revision: {
      ...sampleLawViewerDocument.revision,
      lawId: "000AC0000000001",
      revisionId: "000AC0000000001_rev",
    },
    nodes: bulkyNodes,
  });

  renderLawViewerRoute(
    "/laws/129AC0000000089",
    createFixtureRepository().repository,
    storage.repository,
  );

  expect(await screen.findByRole("article", { name: "民法" })).toBeInTheDocument();

  // 上限を超えたので、より古い法令が落ちる。
  await waitFor(async () => {
    await expect(storage.repository.getLawDocument("000AC0000000001")).resolves.toBeUndefined();
  });
});
```

`bulkyNodes` の 200 × 1,000 文字は UTF-8 で約 600 KB にしかならない。25 MB を超えさせるには `length` を 10_000 程度まで上げる必要があり、テストが遅くなる。そこで **このテストだけ `STORAGE_LIMIT_STORAGE_KEY` を使わず、`useStorageLimit` をモックして小さい上限を注入する**。`vi.mock("./use-storage-limit", ...)` で `limitBytes: 1_000` を返させ、`bulkyNodes` は上のままにする。上限値そのものの正しさは Task 1 のテストが持っており、ここで重ねて検証しない。

- [ ] **Step 3: テストが落ちることを確認する**

```bash
pnpm exec vitest run --dir src src/app/law-viewer-page.test.tsx
```

期待: FAIL。古い法令が残ったまま。

- [ ] **Step 4: ビューアに配線する**

`src/app/law-viewer-page.tsx` の `LawViewerPageLoader` で `useStorageLimit` を呼び、`createSavedLawUseCase` に渡す。

```ts
const { limitBytes } = useStorageLimit();
const savedLawUseCase = useMemo(
  () => createSavedLawUseCase(storageRepository, { getStorageLimitBytes: () => limitBytes }),
  [limitBytes, storageRepository],
);
```

`LawViewerReadyState` 側にも同じ `savedLawUseCase` が渡っているか確認し、渡っていなければ props で受け取る形に揃える。ユースケースを 2 箇所で別々に作らない（上限の設定が片方にしか効かなくなる）。

- [ ] **Step 5: テストが通ることを確認する**

```bash
pnpm run typecheck && pnpm exec vitest run --dir src
```

期待: 型エラーなし、全テスト PASS。

- [ ] **Step 6: コミット**

```bash
git add src/app/use-storage-limit.ts src/app/law-viewer-page.tsx src/app/law-viewer-page.test.tsx
git commit -m "feat: ビューアの自動保存に容量上限を適用する"
```

---

### Task 6: ビューアの表示を「ダウンロード」に改める

**Files:**

- Modify: `src/app/law-viewer-page.tsx`
- Test: `src/app/law-viewer-page.test.tsx`

**Interfaces:**

- Consumes: なし
- Produces: なし（表示のみ）

- [ ] **Step 1: 既存テストの文言を差し替え、失敗を確認する**

`src/app/law-viewer-page.test.tsx` の「ピン留め」「ピン留めを解除」「ピン留め中」を次に読み替える。

- ボタン名 `"ピン留め"` → `"ダウンロード"`
- ボタン名 `"ピン留めを解除"` → `"ダウンロード済み"`
- テキスト `"ピン留め中"` の検証は削除する（バッジを廃止するため）
- エラー文言 `"ピン留めできませんでした。端末の保存領域を確認してください。"` → `"ダウンロードできませんでした。端末の保存領域を確認してください。"`
- エラー文言 `"ピン留めを解除できませんでした。時間をおいて再試行してください。"` → `"ダウンロードを取り消せませんでした。時間をおいて再試行してください。"`

```bash
pnpm exec vitest run --dir src src/app/law-viewer-page.test.tsx
```

期待: FAIL。旧文言のままのため要素が見つからない。

- [ ] **Step 2: バッジを削除する**

`src/app/law-viewer-page.tsx` の次のブロックを削除する。ボタン自身が状態を表すため重複する。

```tsx
{
  savedState.isPinned ? (
    <Badge variant="secondary" className="w-fit">
      ピン留め中
    </Badge>
  ) : null;
}
```

`Badge` が他で使われていなければ import も削除する。

- [ ] **Step 3: ボタンを差し替える**

`lucide-react` の import を `Pin` / `PinOff` から `CircleCheck` / `Download` に差し替える。ボタンを次にする。

```tsx
<Button
  aria-describedby={saveError === undefined ? undefined : saveErrorId}
  // 済みの状態は既存の主色で示す。このテーマの --primary は #166534
  // （ダークは #4ade80）で既に緑なので、成功色のトークンを足す必要がない。
  className={cn("w-fit gap-2", savedState.isPinned && "text-primary")}
  disabled={isSaving}
  onClick={() => {
    void handleLawPinToggle();
  }}
  type="button"
  variant={savedState.isPinned ? "outline" : "default"}
>
  {savedState.isPinned ? (
    <CircleCheck className="size-4" aria-hidden="true" />
  ) : (
    <Download className="size-4" aria-hidden="true" />
  )}
  {savedState.isPinned ? "ダウンロード済み" : "ダウンロード"}
</Button>
```

新しい色トークンは追加しない。`src/index.css` に `--success` 系は存在しないが、`--primary` が既に緑なので `text-primary` で足りる。トークンを増やさない方がテーマ統一の方針（`2026-07-16-theme-unification-design.md`）に沿う。

`cn` は `@/shared/utils/cn` から import する（未 import の場合）。

もう 1 箇所のボタン（モバイル側、`savedState.isPinned ? "ピン留めを解除" : "ピン留め"`）も同じ形に揃える。

- [ ] **Step 4: エラー文言を差し替える**

```ts
setSaveError(
  savedState.isPinned
    ? "ダウンロードを取り消せませんでした。時間をおいて再試行してください。"
    : "ダウンロードできませんでした。端末の保存領域を確認してください。",
);
```

- [ ] **Step 5: テストが通ることを確認する**

```bash
pnpm run typecheck && pnpm exec vitest run --dir src
```

期待: 型エラーなし、全テスト PASS。

- [ ] **Step 6: コミット**

```bash
git add src/app/law-viewer-page.tsx src/app/law-viewer-page.test.tsx
git commit -m "feat: ビューアのピン留め表示をダウンロードに改める"
```

---

### Task 7: 保存リストの容量表示と使用量メーター

**Files:**

- Create: `src/shared/utils/bytes.ts`
- Create: `src/shared/utils/bytes.test.ts`
- Modify: `src/app/saved-page.tsx`
- Test: `src/app/saved-page.test.tsx`

**Interfaces:**

- Consumes: Task 1 の `useStorageLimit`、Task 2 の `SavedLawSummary.byteSize`
- Produces: `formatByteSize(bytes: number): string`

- [ ] **Step 1: 整形ユーティリティのテストを書く**

`src/shared/utils/bytes.test.ts`:

```ts
import { describe, expect, it } from "vitest";

import { formatByteSize } from "./bytes";

describe("formatByteSize", () => {
  it("uses KB below one megabyte", () => {
    expect(formatByteSize(394_240)).toBe("385 KB");
  });

  it("keeps one decimal place for megabytes", () => {
    expect(formatByteSize(1_677_722)).toBe("1.6 MB");
  });

  it("shows zero without a fractional part", () => {
    expect(formatByteSize(0)).toBe("0 KB");
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

```bash
pnpm exec vitest run --dir src src/shared/utils/bytes.test.ts
```

期待: FAIL。`./bytes` が存在しない。

- [ ] **Step 3: 実装する**

`src/shared/utils/bytes.ts`:

```ts
const bytesPerKilobyte = 1024;
const bytesPerMegabyte = bytesPerKilobyte * 1024;

/**
 * 保存容量の表示用。バイト単位は細かすぎ、GB に届く想定も無いため KB と MB だけを使う。
 * MB は小数第 1 位まで出す。法令 1 件が 0.4〜3 MB の範囲に収まり、整数だと差が潰れるため。
 */
export const formatByteSize = (bytes: number): string => {
  if (bytes < bytesPerMegabyte) {
    return `${Math.round(bytes / bytesPerKilobyte).toLocaleString("ja-JP")} KB`;
  }

  return `${(bytes / bytesPerMegabyte).toFixed(1)} MB`;
};
```

- [ ] **Step 4: 保存リストの失敗するテストを書く**

`src/app/saved-page.test.tsx` に追加し、既存テストの見出し文言を差し替える。

- `screen.findByRole("region", { name: "ピン留めした法令" })` → `{ name: "ダウンロード済み" }`
- 空文言 `"ピン留めした法令はまだありません。"` → `"ダウンロードした法令はまだありません。"`

```tsx
it("shows the stored size of each law and the total against the limit", async () => {
  const storage = createMemoryStorageRepository();

  await storage.repository.saveLawDocument({
    law: sampleLawViewerDocument.law,
    nodes: sampleLawViewerDocument.nodes,
    revision: sampleLawViewerDocument.revision,
  });

  renderSavedRoute("/saved", storage.repository);

  const recentSection = await screen.findByRole("region", { name: "最近開いた法令" });

  // ノード数ではなく容量を出す。ノード数は判断材料にならない。
  expect(within(recentSection).getByText(/KB|MB/)).toBeInTheDocument();
  expect(within(recentSection).queryByText(/ノード/)).not.toBeInTheDocument();

  // 合計と上限を出す。上限だけあって使用量が見えないと「いつか勝手に消える」としか読めない。
  expect(screen.getByText(/\/ 50 MB/)).toBeInTheDocument();
});
```

- [ ] **Step 5: テストが落ちることを確認する**

```bash
pnpm exec vitest run --dir src src/app/saved-page.test.tsx
```

期待: FAIL。容量表示と合計が無い。

- [ ] **Step 6: 保存リストを書き換える**

`src/app/saved-page.tsx` で次を行う。

1. `import { formatByteSize } from "@/shared/utils/bytes";` と `import { useStorageLimit } from "./use-storage-limit";` を足す。
2. `SavedPage` で `const { limitBytes } = useStorageLimit();` を呼ぶ。
3. 合計を導出する。

```tsx
const usedBytes = savedLaws.reduce((sum, savedLaw) => sum + (savedLaw.byteSize ?? 0), 0);
const pinnedBytes = savedLaws
  .filter((savedLaw) => pinnedAtByLawId.has(savedLaw.law.lawId))
  .reduce((sum, savedLaw) => sum + (savedLaw.byteSize ?? 0), 0);
```

4. 見出しの下（`ErrorMessage` の並びの前）に使用量を出す。

```tsx
<p className="text-sm text-muted-foreground">
  オフライン保存: {formatByteSize(usedBytes)} / {formatByteSize(limitBytes)}
  <span className="ml-2">
    （ダウンロード済み {formatByteSize(pinnedBytes)} ・ 最近開いた{" "}
    {formatByteSize(usedBytes - pinnedBytes)}）
  </span>
</p>
```

5. `SavedLawList` の行からノード数を消し、容量を出す。

```tsx
<div className="flex flex-wrap gap-2 text-sm text-muted-foreground">
  <span>最終取得: {formatIsoDateLabel(savedLaw.revision.fetchedAt)}</span>
  {savedLaw.byteSize === undefined ? null : <span>{formatByteSize(savedLaw.byteSize)}</span>}
</div>
```

6. セクションの見出しと空文言を差し替える。

```tsx
<SavedLawList
  emptyMessage="ダウンロードした法令はまだありません。"
  headingId="pinned-laws-heading"
  icon={CircleCheck}
  savedLaws={toPinnedSavedLaws(savedLaws, pinnedAtByLawId)}
  title="ダウンロード済み"
/>
```

「最近開いた法令」側は見出しをそのままにし、セクションの説明文で順序の意味を開示する。`SavedLawList` に `description?: string` を足し、見出しの下に `text-sm text-muted-foreground` で出す。

```tsx
description = "空き容量が足りなくなると、下にあるものから削除されます。";
```

`icon` の import を `Archive` に加えて `CircleCheck` を足す（`Pin` は不要になるので削除）。

- [ ] **Step 7: テストが通ることを確認する**

```bash
pnpm run typecheck && pnpm exec vitest run --dir src
```

期待: 型エラーなし、全テスト PASS。

- [ ] **Step 8: コミット**

```bash
git add src/shared/utils/bytes.ts src/shared/utils/bytes.test.ts src/app/saved-page.tsx src/app/saved-page.test.tsx
git commit -m "feat: 保存リストに法令ごとの容量と使用量を表示する"
```

---

### Task 8: 手動削除の導線

**Files:**

- Modify: `src/app/saved-page.tsx`
- Test: `src/app/saved-page.test.tsx`

**Interfaces:**

- Consumes: `SavedLawUseCase.remove`
- Produces: なし

- [ ] **Step 1: 失敗するテストを書く**

```tsx
it("deletes a saved law after confirming", async () => {
  const storage = createMemoryStorageRepository();

  await storage.repository.saveLawDocument({
    law: sampleLawViewerDocument.law,
    nodes: sampleLawViewerDocument.nodes,
    revision: sampleLawViewerDocument.revision,
  });

  const { user } = renderSavedRoute("/saved", storage.repository);

  expect(await screen.findByRole("link", { name: "民法" })).toBeInTheDocument();

  await user.click(screen.getByRole("button", { name: "民法を削除" }));
  await user.click(await screen.findByRole("button", { name: "削除する" }));

  await waitFor(async () => {
    await expect(storage.repository.getLawDocument("129AC0000000089")).resolves.toBeUndefined();
  });
  expect(screen.queryByRole("link", { name: "民法" })).not.toBeInTheDocument();
});

it("keeps the law when the confirmation is dismissed", async () => {
  const storage = createMemoryStorageRepository();

  await storage.repository.saveLawDocument({
    law: sampleLawViewerDocument.law,
    nodes: sampleLawViewerDocument.nodes,
    revision: sampleLawViewerDocument.revision,
  });

  const { user } = renderSavedRoute("/saved", storage.repository);

  expect(await screen.findByRole("link", { name: "民法" })).toBeInTheDocument();

  await user.click(screen.getByRole("button", { name: "民法を削除" }));
  await user.click(await screen.findByRole("button", { name: "キャンセル" }));

  await expect(storage.repository.getLawDocument("129AC0000000089")).resolves.toBeDefined();
});
```

- [ ] **Step 2: テストが落ちることを確認する**

```bash
pnpm exec vitest run --dir src src/app/saved-page.test.tsx
```

期待: FAIL。削除ボタンが無い。

- [ ] **Step 3: 削除ボタンと確認ダイアログを足す**

`SavedLawList` の各行の右端にアイコンボタンを置く。

```tsx
<Button
  aria-label={`${savedLaw.law.title}を削除`}
  className="shrink-0"
  onClick={() => {
    onRequestDelete(savedLaw);
  }}
  size="icon"
  type="button"
  variant="ghost"
>
  <Trash2 className="size-4" aria-hidden="true" />
</Button>
```

`SavedPage` に確認ダイアログを足す。`src/shared/ui/dialog.tsx` を `AnchorCompareDialog.tsx` と同じ形で使う（`AlertDialog` はこのリポジトリに無い）。削除対象が決まったときだけマウントするので、開閉フラグは別に持たない。

```tsx
{
  lawPendingDeletion === undefined ? null : (
    <Dialog
      open
      onOpenChange={(open) => {
        if (!open) {
          setLawPendingDeletion(undefined);
        }
      }}
    >
      <DialogContent>
        <DialogHeader>
          <DialogTitle>{lawPendingDeletion.law.title}を削除</DialogTitle>
          <DialogDescription>
            この法令の保存データを削除します。もう一度開けば再取得されます。
          </DialogDescription>
        </DialogHeader>
        <DialogFooter>
          <Button
            onClick={() => {
              void handleConfirmDelete();
            }}
            type="button"
            variant="destructive"
          >
            削除する
          </Button>
          <Button
            onClick={() => {
              setLawPendingDeletion(undefined);
            }}
            type="button"
            variant="outline"
          >
            キャンセル
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```

```tsx
const [lawPendingDeletion, setLawPendingDeletion] = useState<SavedLawSummary | undefined>();

const handleConfirmDelete = async () => {
  if (lawPendingDeletion === undefined) {
    return;
  }

  try {
    await savedLawUseCase.remove(lawPendingDeletion.law.lawId);
    setLawPendingDeletion(undefined);
    await reload();
  } catch {
    // 明示操作なのでバナーを出す。
    setError("法令を削除できませんでした。時間をおいて再試行してください。");
  }
};
```

`reload` は既存の `loadSavedPageData` / `applySavedPageData` を再利用する形にする。既存の `useEffect` が使っている読み込み処理を関数に切り出し、削除後にも呼べるようにする。

ダイアログ本文は「この法令の保存データを削除します。もう一度開けば再取得されます。」とし、ボタンは「削除する」「キャンセル」にする。

- [ ] **Step 4: テストが通ることを確認する**

```bash
pnpm run typecheck && pnpm exec vitest run --dir src
```

期待: 型エラーなし、全テスト PASS。

- [ ] **Step 5: コミット**

```bash
git add src/app/saved-page.tsx src/app/saved-page.test.tsx
git commit -m "feat: 保存リストから法令を削除できるようにする"
```

---

### Task 9: `persist()` の要求と設定画面

**Files:**

- Create: `src/core/storage/persistence.ts`
- Create: `src/core/storage/persistence.test.ts`
- Modify: `src/core/storage/index.ts`
- Modify: `src/app/law-viewer-page.tsx`
- Modify: `src/app/settings-page.tsx`
- Test: `src/app/settings-page.test.tsx`

**Interfaces:**

- Consumes: Task 1 の設定ストア
- Produces:
  - `isStoragePersisted(): Promise<boolean>`
  - `requestStoragePersistence(): Promise<boolean>`
  - `estimateStorageUsage(): Promise<{ usage?: number; quota?: number } | undefined>`
  - `PERSISTENCE_REQUESTED_STORAGE_KEY: string`

- [ ] **Step 1: 失敗するテストを書く**

`src/core/storage/persistence.test.ts`:

```ts
import { afterEach, describe, expect, it, vi } from "vitest";

import { estimateStorageUsage, isStoragePersisted, requestStoragePersistence } from "./persistence";

afterEach(() => {
  vi.unstubAllGlobals();
});

describe("storage persistence", () => {
  it("reports not persisted when the API is unavailable", async () => {
    vi.stubGlobal("navigator", {});

    await expect(isStoragePersisted()).resolves.toBe(false);
  });

  it("reports not persisted when the API throws", async () => {
    // 保護の状態が読めないことで閲覧を止めない。
    vi.stubGlobal("navigator", {
      storage: { persisted: () => Promise.reject(new Error("denied")) },
    });

    await expect(isStoragePersisted()).resolves.toBe(false);
  });

  it("returns the grant result of the request", async () => {
    vi.stubGlobal("navigator", { storage: { persist: () => Promise.resolve(true) } });

    await expect(requestStoragePersistence()).resolves.toBe(true);
  });

  it("returns undefined when usage cannot be estimated", async () => {
    vi.stubGlobal("navigator", {});

    await expect(estimateStorageUsage()).resolves.toBeUndefined();
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

```bash
pnpm exec vitest run --dir src src/core/storage/persistence.test.ts
```

期待: FAIL。`./persistence` が存在しない。

- [ ] **Step 3: 実装する**

`src/core/storage/persistence.ts`:

```ts
// 一度でも要求したかを持つ。初回のダウンロード指定でだけ要求し、以後は設定画面に委ねる。
// Firefox は persist() でプロンプトを出すため、毎回呼ぶと閲覧の邪魔になる。
export const PERSISTENCE_REQUESTED_STORAGE_KEY = "surasura:storage:persist-requested";

const getStorageManager = (): StorageManager | undefined =>
  typeof navigator === "undefined" ? undefined : navigator.storage;

/** 保護されているか。読めないときは false を返し、閲覧を止めない。 */
export const isStoragePersisted = async (): Promise<boolean> => {
  const storage = getStorageManager();

  if (storage?.persisted === undefined) {
    return false;
  }

  try {
    return await storage.persisted();
  } catch {
    return false;
  }
};

/** 保護を要求する。拒否・失敗しても false を返すだけで、呼び出し側はバナーを出さない。 */
export const requestStoragePersistence = async (): Promise<boolean> => {
  const storage = getStorageManager();

  if (storage?.persist === undefined) {
    return false;
  }

  try {
    return await storage.persist();
  } catch {
    return false;
  }
};

/** オリジン全体の使用量。取れないときは undefined を返し、表示側で欄を空にする。 */
export const estimateStorageUsage = async (): Promise<
  { usage?: number; quota?: number } | undefined
> => {
  const storage = getStorageManager();

  if (storage?.estimate === undefined) {
    return undefined;
  }

  try {
    const estimate = await storage.estimate();

    return { usage: estimate.usage, quota: estimate.quota };
  } catch {
    return undefined;
  }
};
```

`src/core/storage/index.ts` に追加する。

```ts
export {
  estimateStorageUsage,
  isStoragePersisted,
  PERSISTENCE_REQUESTED_STORAGE_KEY,
  requestStoragePersistence,
} from "./persistence";
```

- [ ] **Step 4: 初回のダウンロード指定で要求する**

`src/app/law-viewer-page.tsx` の `handleLawPinToggle` で、ピンを立てたあとに一度だけ要求する。

```ts
setSavedState((previous) => ({ ...previous, isPinned: true }));

// ダウンロードは「これを残しておきたい」という最も明確な意思表示であり、
// Firefox のプロンプトが出ても文脈が通る唯一の瞬間。以後は設定画面に委ねる。
if (window.localStorage.getItem(PERSISTENCE_REQUESTED_STORAGE_KEY) === null) {
  window.localStorage.setItem(PERSISTENCE_REQUESTED_STORAGE_KEY, "1");
  void requestStoragePersistence();
}
```

拒否されてもバナーは出さない。

- [ ] **Step 5: 設定画面に失敗するテストを書く**

`src/app/settings-page.test.tsx` に追加する。

```tsx
it("shows the persistence state and lets the user request it", async () => {
  const persist = vi.fn(() => Promise.resolve(true));

  vi.stubGlobal("navigator", {
    ...navigator,
    storage: {
      persisted: () => Promise.resolve(false),
      persist,
      estimate: () => Promise.resolve({ usage: 1_048_576, quota: 104_857_600 }),
    },
  });

  const { user } = renderSettingsRoute();

  expect(await screen.findByText("保護されていません")).toBeInTheDocument();

  await user.click(screen.getByRole("button", { name: "保護を有効にする" }));

  expect(persist).toHaveBeenCalled();
  expect(await screen.findByText("保護されています")).toBeInTheDocument();
});

it("lets the user choose the offline storage limit", async () => {
  const { user } = renderSettingsRoute();

  await user.selectOptions(await screen.findByLabelText("オフライン保存の上限"), "100");

  expect(window.localStorage.getItem(STORAGE_LIMIT_STORAGE_KEY)).toBe("100");
});
```

`renderSettingsRoute` は同ファイルの既存ヘルパーに倣う。無ければ既存の描画方法をそのまま使う。

- [ ] **Step 6: テストが落ちることを確認する**

```bash
pnpm exec vitest run --dir src src/app/settings-page.test.tsx
```

期待: FAIL。該当のセクションが無い。

- [ ] **Step 7: 設定画面にセクションを足す**

「オフライン保存」セクションを追加する。含める項目は次の 3 つ。

1. **上限のセレクト**: `selectableStorageLimits` を `<option>` にし、`useStorageLimit().setLimitMegabytes` で更新する。ラベルは「オフライン保存の上限」。
2. **保護の状態**: `isStoragePersisted()` の結果で「保護されています」/「保護されていません」を出す。未保護のときだけ「保護を有効にする」ボタンを出し、`requestStoragePersistence()` を呼んで状態を更新する。
3. **オリジン全体の使用量**: `estimateStorageUsage()` の `usage` を `formatByteSize` で出す。`undefined` のときは欄を空にする。

未保護のときの説明文に次を添える。ブラウザ判定はしない（出し分けは壊れやすい）。

> ブラウザは空き容量が足りなくなると、このアプリのデータをまとめて削除することがあります。ホーム画面に追加しておくと、削除されにくくなります。

状態の読み取りは開いたときに 1 回行う。`useEffect` の同期本体で `setState` を呼ぶと `react-hooks` の `set-state-in-effect`（error）に触れるため、非同期の解決後に更新する形にする。`saved-page.tsx` の読み込み処理と同じ骨格である。

```tsx
const [isPersisted, setIsPersisted] = useState(false);
const [originUsage, setOriginUsage] = useState<number | undefined>();

useEffect(() => {
  let isActive = true;

  void isStoragePersisted().then((persisted) => {
    if (isActive) {
      setIsPersisted(persisted);
    }
  });
  void estimateStorageUsage().then((estimate) => {
    if (isActive) {
      setOriginUsage(estimate?.usage);
    }
  });

  return () => {
    isActive = false;
  };
}, []);

const handleRequestPersistence = async () => {
  // 拒否されてもバナーは出さない。状態表示が変わらないことで伝わる。
  setIsPersisted(await requestStoragePersistence());
};
```

マークアップは既存の設定セクションに倣う。

```tsx
<section aria-labelledby={offlineHeadingId} className="grid gap-3">
  <h2 id={offlineHeadingId} className="text-lg font-semibold text-foreground">
    オフライン保存
  </h2>

  <label className="grid gap-1 text-sm" htmlFor={storageLimitSelectId}>
    オフライン保存の上限
    <select
      id={storageLimitSelectId}
      onChange={(event) => {
        setLimitMegabytes(Number(event.target.value) as StorageLimitMegabytes);
      }}
      value={limitMegabytes}
    >
      {selectableStorageLimits.map((limit) => (
        <option key={limit} value={limit}>
          {limit} MB
        </option>
      ))}
    </select>
  </label>

  <p className="text-sm text-muted-foreground">
    {isPersisted ? "保護されています" : "保護されていません"}
  </p>
  {isPersisted ? null : (
    <>
      <p className="text-sm text-muted-foreground">
        ブラウザは空き容量が足りなくなると、このアプリのデータをまとめて削除することがあります。
        ホーム画面に追加しておくと、削除されにくくなります。
      </p>
      <Button
        className="w-fit"
        onClick={() => {
          void handleRequestPersistence();
        }}
        type="button"
        variant="outline"
      >
        保護を有効にする
      </Button>
    </>
  )}

  {originUsage === undefined ? null : (
    <p className="text-sm text-muted-foreground">
      このアプリ全体の使用量: {formatByteSize(originUsage)}
    </p>
  )}
</section>
```

`offlineHeadingId` と `storageLimitSelectId` は既存の `useId()` の並びに足す。`select` の書き方は同ファイルの年度セレクタに倣う（`src/shared/ui/select.tsx` ではなくネイティブ `select` を使っている）。

- [ ] **Step 8: テストが通ることを確認する**

```bash
pnpm run typecheck && pnpm exec vitest run --dir src
```

期待: 型エラーなし、全テスト PASS。

- [ ] **Step 9: コミット**

```bash
git add src/core/storage/persistence.ts src/core/storage/persistence.test.ts src/core/storage/index.ts src/app/law-viewer-page.tsx src/app/settings-page.tsx src/app/settings-page.test.tsx
git commit -m "feat: 保存データの保護を要求し、設定画面で状態と上限を扱う"
```

---

### Task 10: 全体検証と PR

**Files:** なし（検証のみ）

- [ ] **Step 1: 検証ゲートを通す**

```bash
pnpm run typecheck && pnpm exec vitest run --dir src && pnpm run lint && pnpm exec prettier --check src docs
```

期待: 型エラーなし、全テスト PASS、lint の新規指摘なし、整形済み。

`src/app/law-viewer-page.tsx` や `src/app/saved-page.tsx` に「ピン留め」という表記が残っていないことを確認する。内部識別子（`isPinned` / `pinnedAtByLawId` など）は残ってよい。

```bash
grep -rn "ピン留め" src | grep -v "\.test\."
```

期待: 0 件。

- [ ] **Step 2: preview build で実画面を確認する**

dev サーバではなく preview build を使う（HMR と依存再最適化がフルリロードを起こし、検証を中断させるため）。

```bash
pnpm run build && pnpm run preview
```

- [ ] **Step 3: playwright-cli で証跡を撮る**

システムにインストールされた `playwright-cli` を使う。`open` には `--browser chromium` が必要で、撮影は `--filename` に絶対パスを渡す。撮るもの:

1. 法令を開いた直後（ボタンが「ダウンロード」）
2. ダウンロード後（丸囲みチェックと「ダウンロード済み」）
3. 保存リスト（法令ごとの容量、使用量と内訳、2 セクション）
4. 手動削除の確認ダイアログと削除後の状態
5. 設定画面の「オフライン保存」セクション（上限、保護の状態、使用量）
6. 上限を小さくした状態で複数の法令を開き、古いものが消えること

連続操作は `video-start` で録画し、GitHub へ貼るときはアニメーション WebP に変換する（GitHub は WebM を受け付けない）。

```bash
ffmpeg -y -i rec.webm -vf "crop=640:450:0:0,fps=8,scale=1280:-2:flags=lanczos" -c:v libwebp_anim -lossless 0 -q:v 60 -loop 0 -an rec.webp
```

- [ ] **Step 4: Antigravity レビューを走らせる**

```bash
pnpm run review:antigravity
```

APPROVE になるまで指摘に対応する。妥当でないと判断した指摘は、対応しない理由を PR 本文に書く。

- [ ] **Step 5: PR を作る**

base は `main`（PR 2 はマージ済みなので stacked にしない）。PR 本文には次を含める。

- 何を変えたか（`byteSize`、2 段階のエビクション、`persist()`、可視化、手動削除、UI 語彙）
- 目的の再定義（容量ではなく「消され方」が制約であること、守る対象はユーザー資産であること）
- 振る舞いの変化（保存法令が自動削除されること、上限の既定値、「ピン留め」表記の廃止）
- 検証（テスト・静的解析の出力、実画面のスクリーンショットと録画）
- `Close #197`（PR 3 で Issue を閉じる）

作成後に自分を Assign する。

---

### Task 11: 同一版の再保存で本文を書き直さない

> **実行順**: Task 2 の直後に行う。同じ `saveLawDocument` を触るため、離すと二度手間になる。

PR 2 の申し送り（書き込み増幅）を解消する。現状は同じ版を開き直すたびに、その版のノードを全件削除してから全件入れ直している。民法なら毎回 4,283 件の削除と挿入が走る。自動保存が入った PR 2 以降、これは法令を開くたびに発生する。

**Files:**

- Modify: `src/core/storage/repository.ts`
- Test: `src/core/storage/repository.test.ts`

**Interfaces:**

- Consumes: Task 2 の `SavedLawRecord.byteSize`
- Produces: `shouldRewriteRevisionNodes(existing: SavedLawRecord | undefined, byteSize: number, nodeCount: number): boolean`（同ファイル内の非公開ヘルパー。テストのために export する）

- [ ] **Step 1: 失敗するテストを書く**

`src/core/storage/repository.test.ts` に追加する。

```ts
describe("shouldRewriteRevisionNodes", () => {
  const stored = {
    lawId: "129AC0000000089",
    revisionId: "129AC0000000089_rev",
    isCurrent: 1 as const,
    nodeCount: 2,
    savedAt: "2026-07-06T00:00:00.000Z",
    updatedAt: "2026-07-06T00:00:00.000Z",
    byteSize: 1_000,
  };

  it("rewrites when the revision has never been saved", () => {
    expect(shouldRewriteRevisionNodes(undefined, 1_000, 2)).toBe(true);
  });

  it("rewrites when the stored record predates size tracking", () => {
    // byteSize を持たないレコードは中身を照合できないので、安全側に倒して書き直す。
    const { byteSize: _byteSize, ...withoutSize } = stored;

    expect(shouldRewriteRevisionNodes(withoutSize, 1_000, 2)).toBe(true);
  });

  it("rewrites when the normalized text changed even though the revision id did not", () => {
    // e-Gov の版が同じでも、こちらの正規化が変われば本文は変わる。
    // revisionId だけで判断すると、アプリ更新後に古い正規化結果が残り続ける。
    expect(shouldRewriteRevisionNodes(stored, 1_200, 2)).toBe(true);
    expect(shouldRewriteRevisionNodes(stored, 1_000, 3)).toBe(true);
  });

  it("skips the rewrite when the stored content matches", () => {
    expect(shouldRewriteRevisionNodes(stored, 1_000, 2)).toBe(false);
  });
});
```

同じファイルの保存法令の describe に、振る舞いとしての回帰テストも足す。

```ts
it("keeps the document intact when the same revision is saved again", async () => {
  let currentTime = new Date("2026-07-06T00:00:00.000Z");
  const repository = createStorageRepository({
    databaseName: createDatabaseName(),
    now: () => currentTime,
  });

  await repository.saveLawDocument({ law, revision, nodes: [articleNode, paragraphNode] });
  currentTime = new Date("2026-07-09T00:00:00.000Z");
  await repository.saveLawDocument({ law, revision, nodes: [articleNode, paragraphNode] });

  // ノードの書き直しを省いても本文は欠けない。
  await expect(repository.getLawDocument(law.lawId)).resolves.toEqual({
    law,
    revision,
    nodes: [articleNode, paragraphNode],
    savedAt: "2026-07-06T00:00:00.000Z",
  });

  // LRU が使う updatedAt は書き直しの有無に関わらず進む。
  const [record] = await repository.listSavedLawRecords();

  expect(record?.updatedAt).toBe("2026-07-09T00:00:00.000Z");
});
```

- [ ] **Step 2: テストが落ちることを確認する**

```bash
pnpm exec vitest run --dir src src/core/storage/repository.test.ts
```

期待: FAIL。`shouldRewriteRevisionNodes` が存在しない。

- [ ] **Step 3: 実装する**

`src/core/storage/repository.ts` の末尾（他のヘルパーの隣）に追加する。

```ts
/**
 * その版のノードを削除して入れ直す必要があるか。
 *
 * 同じ版を開き直すたびに全ノードを書き直すと、民法なら毎回 4,283 件の削除と挿入が走る。
 * 自動保存があるため、これは法令を開くたびに発生する。
 *
 * 判断に `revisionId` の一致だけを使ってはいけない。e-Gov の版が同じでも、こちらの
 * 正規化処理が変われば保存すべきノードは変わり、アプリ更新後に古い結果が残り続ける。
 * バイト数とノード数の一致まで見れば、その変化も捕まえられる。
 */
export const shouldRewriteRevisionNodes = (
  existing: SavedLawRecord | undefined,
  byteSize: number,
  nodeCount: number,
): boolean =>
  existing === undefined ||
  // 記録以前のレコードは中身を照合できないため、安全側に倒す。
  existing.byteSize === undefined ||
  existing.byteSize !== byteSize ||
  existing.nodeCount !== nodeCount;
```

`saveLawDocument` の該当箇所を差し替える。`byteSize` の計算を `deleteRevisionNodes` より前に移す。

```ts
const byteSize = measureNodesByteSize(document.nodes);
const rewritesNodes = shouldRewriteRevisionNodes(existing, byteSize, document.nodes.length);

// 同じ版を書き直すときだけ、その版のノードを消して入れ直す。
// 中身が変わっていなければ、その入れ替えも省く。
if (rewritesNodes) {
  await deleteRevisionNodes(nodes, lawId, revisionId);
}
```

ノードの投入も同じ条件で囲む。

```ts
if (rewritesNodes) {
  for (const [sortOrder, node] of document.nodes.entries()) {
    void nodes.put({
      id: node.id,
      lawId: node.lawId,
      revisionId: node.revisionId,
      sortOrder,
      node,
    });
  }
}
```

`savedLaws.put` は条件に関わらず実行する。`updatedAt` は LRU の順序を決めるため、本文を書き直さない場合でも進める必要がある。

- [ ] **Step 4: テストが通ることを確認する**

```bash
pnpm run typecheck && pnpm exec vitest run --dir src
```

期待: 型エラーなし、全テスト PASS。

- [ ] **Step 5: コミット**

```bash
git add src/core/storage/repository.ts src/core/storage/repository.test.ts
git commit -m "perf: 内容が変わらない再保存でノードを書き直さない"
```

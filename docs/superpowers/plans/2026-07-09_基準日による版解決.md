# 基準日による版解決 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 学習年度の基準日（グローバル設定）に基づき、e-Gov API v2 の `asof` でビューワーの表示版を「施行日 ≤ 基準日 の最新版」に解決し、解決に使った基準日を表示する。

**Architecture:** 版解決はサーバー（e-Gov `asof`）に委ねる。クライアントは (1) 基準日を `localStorage` に永続化する小さな純ストア、(2) それを購読する React フック、(3) 基準日を `asof` としてビューワーのデータ取得へ配線、(4) 解決結果をツールバー／フッターに表示、の4層で構成する。

**Tech Stack:** TypeScript / React 19 / TanStack Router / Vitest + Testing Library / Tailwind / idb（既存）。

## Global Constraints

- パッケージマネージャは `pnpm@11.10.0`。スクリプト実行は `pnpm run <script>`。
- コミットは Conventional Commits。フッターに `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` を必ず付ける。
- コード内コメントは日本語。ログ・エラーメッセージ・UI 文言以外の識別子は英語。UI 文言は日本語。
- 検証ゲート（各タスクのコミット前に必須・すべて成功で通過）:
  - `pnpm test`
  - `pnpm typecheck`
  - `pnpm lint`
  - `pnpm run format:check`（prettier。空行・整形崩れでも落ちるため必ず実行）
- import 順は「外部パッケージ → `@/…` エイリアス → 相対 `./…`」。パスエイリアス `@/` は `src/`。
- e-Gov API v2 の `asof` 下限は **2017-04-01**（それ未満は HTTP 400）。基準日入力・検証はこの下限を守る。
- 各 core モジュールは `index.ts` バレルで公開面を持つ（既存 `@/core/egov` 等に倣う）。

---

## File Structure

**新規作成:**

- `src/core/settings/base-date.ts` — 基準日の永続化・検証・購読・`asof` 解決（純ロジック、React 非依存）。
- `src/core/settings/base-date.test.ts` — 上記の単体テスト。
- `src/core/settings/index.ts` — バレル公開。
- `src/app/use-base-date.ts` — `useSyncExternalStore` で基準日ストアを購読する React フック。
- `src/app/use-base-date.test.ts` — フックのテスト。
- `src/app/settings-page.tsx` — 設定画面（`pages.tsx` から抽出し、基準日をインタラクティブ化）。
- `src/app/settings-page.test.tsx` — 設定画面のテスト。
- `docs/adr/2026-07-09-egov-asof-server-side-resolution.md` — API 検証結果の ADR。

**変更:**

- `src/app/law-viewer-sample.ts` — `LawViewerDocument` に `requestedAsOf?` を追加。
- `src/app/law-viewer-loader.ts` — `asOf` 引数を追加し `getLaw` へ配線、`requestedAsOf` を状態へ載せ、基準日起因エラー文言を追加。
- `src/app/law-viewer-loader.test.ts` — `asOf` 伝播・`requestedAsOf`・基準日エラーのテストを追加。
- `src/app/law-viewer-page.tsx` — `useBaseDate` を読み `asof` を配線、ツールバー／フッターに基準日表示。
- `src/app/law-viewer-page.test.tsx` — 表示と版切替のテストを追加。
- `src/app/pages.tsx` — `SettingsPage` 実装を削除し `settings-page.tsx` を再エクスポート。

---

## Task 1: 基準日ストア（`core/settings`）

**Files:**

- Create: `src/core/settings/base-date.ts`
- Create: `src/core/settings/index.ts`
- Test: `src/core/settings/base-date.test.ts`

**Interfaces:**

- Produces:
  - `earliestBaseDate: string`（`"2017-04-01"`）
  - `isValidBaseDate(value: string): boolean`
  - `getBaseDate(): string | undefined`
  - `setBaseDate(value: string | undefined): void`
  - `subscribe(listener: () => void): () => void`
  - `resolveAsOf(baseDate: string | undefined): string | undefined`

- [ ] **Step 1: Write the failing test**

Create `src/core/settings/base-date.test.ts`:

```ts
import { afterEach, describe, expect, it, vi } from "vitest";

import {
  earliestBaseDate,
  getBaseDate,
  isValidBaseDate,
  resolveAsOf,
  setBaseDate,
  subscribe,
} from "./base-date";

afterEach(() => {
  localStorage.clear();
});

describe("isValidBaseDate", () => {
  it("accepts an in-range ISO date", () => {
    expect(isValidBaseDate("2020-06-01")).toBe(true);
    expect(isValidBaseDate(earliestBaseDate)).toBe(true);
  });

  it("rejects dates before the e-Gov lower bound", () => {
    expect(isValidBaseDate("2016-12-31")).toBe(false);
  });

  it("rejects malformed or impossible dates", () => {
    expect(isValidBaseDate("2020-6-1")).toBe(false);
    expect(isValidBaseDate("2020-02-31")).toBe(false);
    expect(isValidBaseDate("not-a-date")).toBe(false);
  });
});

describe("getBaseDate / setBaseDate", () => {
  it("persists and reads back a valid base date", () => {
    setBaseDate("2020-06-01");
    expect(getBaseDate()).toBe("2020-06-01");
  });

  it("clears the base date when set to undefined", () => {
    setBaseDate("2020-06-01");
    setBaseDate(undefined);
    expect(getBaseDate()).toBeUndefined();
  });

  it("does not persist an invalid base date", () => {
    setBaseDate("2016-01-01");
    expect(getBaseDate()).toBeUndefined();
  });

  it("treats a corrupted stored value as unset", () => {
    localStorage.setItem("surasura:base-date", "garbage");
    expect(getBaseDate()).toBeUndefined();
  });
});

describe("subscribe", () => {
  it("notifies listeners on change and stops after unsubscribe", () => {
    const listener = vi.fn();
    const unsubscribe = subscribe(listener);

    setBaseDate("2020-06-01");
    expect(listener).toHaveBeenCalledTimes(1);

    unsubscribe();
    setBaseDate("2021-04-01");
    expect(listener).toHaveBeenCalledTimes(1);
  });
});

describe("resolveAsOf", () => {
  it("returns the base date when valid", () => {
    expect(resolveAsOf("2020-06-01")).toBe("2020-06-01");
  });

  it("returns undefined when unset (current law)", () => {
    expect(resolveAsOf(undefined)).toBeUndefined();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test src/core/settings/base-date.test.ts`
Expected: FAIL（`base-date` モジュールが存在しない）。

- [ ] **Step 3: Write minimal implementation**

Create `src/core/settings/base-date.ts`:

```ts
// e-Gov API v2 が asof に受理する最古の日付。これより前は HTTP 400 になる。
export const earliestBaseDate = "2017-04-01";

// localStorage のキー。名前空間を付けて他アプリと衝突しないようにする。
const storageKey = "surasura:base-date";

// 同一タブ内の購読者。別タブは storage イベントで拾う。
const listeners = new Set<() => void>();

// YYYY-MM-DD 形式で、実在する日付かつ earliestBaseDate 以上かを検証する。
export const isValidBaseDate = (value: string): boolean => {
  if (!/^\d{4}-\d{2}-\d{2}$/.test(value)) {
    return false;
  }

  // Date.parse は "2020-02-31" を 3/2 に丸めるため、往復一致で厳密に存在日を確認する。
  const timestamp = Date.parse(`${value}T00:00:00Z`);
  if (Number.isNaN(timestamp)) {
    return false;
  }
  if (new Date(timestamp).toISOString().slice(0, 10) !== value) {
    return false;
  }

  // ISO 8601 の日付は辞書順比較がそのまま日付順になる。
  return value >= earliestBaseDate;
};

export const getBaseDate = (): string | undefined => {
  if (typeof localStorage === "undefined") {
    return undefined;
  }

  const stored = localStorage.getItem(storageKey);
  if (stored === null || !isValidBaseDate(stored)) {
    return undefined;
  }

  return stored;
};

export const setBaseDate = (value: string | undefined): void => {
  if (typeof localStorage === "undefined") {
    return;
  }

  if (value === undefined || value === "") {
    localStorage.removeItem(storageKey);
  } else {
    // 不正値は保存も通知もしない（呼び出し側で検証済みだが二重の防御）。
    if (!isValidBaseDate(value)) {
      return;
    }
    localStorage.setItem(storageKey, value);
  }

  for (const listener of listeners) {
    listener();
  }
};

export const subscribe = (listener: () => void): (() => void) => {
  listeners.add(listener);

  // 別タブでの変更を反映する。key が null の場合は clear() なので常に通知する。
  const handleStorage = (event: StorageEvent) => {
    if (event.key === storageKey || event.key === null) {
      listener();
    }
  };
  if (typeof window !== "undefined") {
    window.addEventListener("storage", handleStorage);
  }

  return () => {
    listeners.delete(listener);
    if (typeof window !== "undefined") {
      window.removeEventListener("storage", handleStorage);
    }
  };
};

// 取得時に e-Gov へ渡す asof を決める。未設定なら undefined（＝現行法）。
export const resolveAsOf = (baseDate: string | undefined): string | undefined =>
  baseDate !== undefined && isValidBaseDate(baseDate) ? baseDate : undefined;
```

Create `src/core/settings/index.ts`:

```ts
export {
  earliestBaseDate,
  getBaseDate,
  isValidBaseDate,
  resolveAsOf,
  setBaseDate,
  subscribe,
} from "./base-date";
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test src/core/settings/base-date.test.ts`
Expected: PASS（全ケース）。

- [ ] **Step 5: Run the verification gate**

Run: `pnpm test src/core/settings/base-date.test.ts && pnpm typecheck && pnpm lint && pnpm run format:check`
Expected: すべて PASS。

- [ ] **Step 6: Commit**

```bash
git add src/core/settings/base-date.ts src/core/settings/index.ts src/core/settings/base-date.test.ts
git commit -m "$(cat <<'EOF'
feat(settings): 基準日のグローバル設定ストアを追加

localStorage に学習年度の基準日を永続化し、購読と asof 解決を提供する。
未設定は現行法（asof 無し）として扱う。

Refs #70

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: 基準日フック（`use-base-date`）

**Files:**

- Create: `src/app/use-base-date.ts`
- Test: `src/app/use-base-date.test.ts`

**Interfaces:**

- Consumes: `getBaseDate`, `setBaseDate`, `subscribe`（`@/core/settings`）
- Produces: `useBaseDate(): { baseDate: string | undefined; setBaseDate: (value: string | undefined) => void }`

- [ ] **Step 1: Write the failing test**

Create `src/app/use-base-date.test.ts`:

```ts
import { act, renderHook } from "@testing-library/react";
import { afterEach, describe, expect, it } from "vitest";

import { setBaseDate as setBaseDateStore } from "@/core/settings";

import { useBaseDate } from "./use-base-date";

afterEach(() => {
  localStorage.clear();
});

describe("useBaseDate", () => {
  it("returns undefined when no base date is stored", () => {
    const { result } = renderHook(() => useBaseDate());

    expect(result.current.baseDate).toBeUndefined();
  });

  it("reflects a stored base date", () => {
    setBaseDateStore("2020-06-01");

    const { result } = renderHook(() => useBaseDate());

    expect(result.current.baseDate).toBe("2020-06-01");
  });

  it("updates when setBaseDate is called through the hook", () => {
    const { result } = renderHook(() => useBaseDate());

    act(() => {
      result.current.setBaseDate("2021-04-01");
    });

    expect(result.current.baseDate).toBe("2021-04-01");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test src/app/use-base-date.test.ts`
Expected: FAIL（`use-base-date` が存在しない）。

- [ ] **Step 3: Write minimal implementation**

Create `src/app/use-base-date.ts`:

```ts
import { useCallback, useSyncExternalStore } from "react";

import { getBaseDate, setBaseDate as setBaseDateStore, subscribe } from "@/core/settings";

// 基準日ストアを React に橋渡しするフック。
// useSyncExternalStore で外部ストア（localStorage）の変更を安全に購読する。
export const useBaseDate = () => {
  const baseDate = useSyncExternalStore(subscribe, getBaseDate, () => undefined);

  const setBaseDate = useCallback((value: string | undefined) => {
    setBaseDateStore(value);
  }, []);

  return { baseDate, setBaseDate };
};
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test src/app/use-base-date.test.ts`
Expected: PASS。

- [ ] **Step 5: Run the verification gate**

Run: `pnpm test src/app/use-base-date.test.ts && pnpm typecheck && pnpm lint && pnpm run format:check`
Expected: すべて PASS。

- [ ] **Step 6: Commit**

```bash
git add src/app/use-base-date.ts src/app/use-base-date.test.ts
git commit -m "$(cat <<'EOF'
feat(settings): 基準日を購読する useBaseDate フックを追加

useSyncExternalStore で基準日ストアを購読し、値と更新関数を返す。

Refs #70

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: ローダーへの `asof` 配線

**Files:**

- Modify: `src/app/law-viewer-sample.ts`（`LawViewerDocument` に `requestedAsOf?` を追加）
- Modify: `src/app/law-viewer-loader.ts`
- Test: `src/app/law-viewer-loader.test.ts`

**Interfaces:**

- Consumes: `LawRepository.getLaw(lawId, { asOf })`（既存 `LawDataQuery.asOf`）
- Produces:
  - `LawViewerDocument.requestedAsOf?: string`
  - `loadLawViewerDocument(lawId, repository?, storageRepository?, asOf?): Promise<LawViewerState>`

- [ ] **Step 1: Write the failing tests**

Add these tests to `src/app/law-viewer-loader.test.ts`（`describe("loadLawViewerDocument", ...)` の末尾、最後の `it` の後に挿入）:

```ts
it("forwards the base date to the repository as an asof query", async () => {
  const { calls, fetcher } = createJsonFetchStub(lawDataFixture);
  const repository = createEgovLawRepository({ fetcher, now });

  const state = await loadLawViewerDocument(
    "129AC0000000089",
    repository,
    createStorageRepositoryStub(),
    "2020-06-01",
  );

  expect(calls).toEqual([
    {
      input:
        "https://laws.e-gov.go.jp/api/2/law_data/129AC0000000089?law_full_text_format=json&asof=2020-06-01&response_format=json",
      init: { headers: { accept: "application/json" } },
    },
  ]);
  expect(state).toMatchObject({ status: "ready", requestedAsOf: "2020-06-01" });
});

it("reports a base-date-specific error when the asof version is unavailable", async () => {
  const { fetcher } = createJsonFetchStub(
    {
      code: "400044",
      message: "法令の時点（asof）には2017-04-01以降を指定してください。",
    },
    400,
  );
  const repository = createEgovLawRepository({ fetcher, now });

  await expect(
    loadLawViewerDocument(
      "129AC0000000089",
      repository,
      createStorageRepositoryStub(),
      "2016-01-01",
    ),
  ).resolves.toEqual({
    status: "error",
    message: "指定した基準日にはこの法令の版が見つかりません。基準日を変更してください。",
  });
});

it("keeps the saved fallback and records the requested base date when offline", async () => {
  const repository = {
    listLaws: (): Promise<LawListResult> => Promise.reject(new Error("Not used in this test")),
    getLaw: (): Promise<LawDocument> => Promise.reject(new Error("network down")),
    getLawMetadata: (): Promise<LawMetadata> => Promise.reject(new Error("Not used in this test")),
  } satisfies LawRepository;
  const storageRepository = createStorageRepositoryStub({
    getLawDocument: vi.fn().mockResolvedValue({
      ...sampleLawViewerDocument,
      savedAt: "2026-07-06T00:00:00.000Z",
    }),
  });

  await expect(
    loadLawViewerDocument("129AC0000000089", repository, storageRepository, "2020-06-01"),
  ).resolves.toMatchObject({
    status: "ready",
    loadedFromStorage: true,
    requestedAsOf: "2020-06-01",
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test src/app/law-viewer-loader.test.ts`
Expected: FAIL（`asOf` 未対応・`requestedAsOf` 無し）。

- [ ] **Step 3: Add `requestedAsOf` to the document type**

In `src/app/law-viewer-sample.ts`, add the field to `LawViewerDocument`:

```ts
export interface LawViewerDocument {
  law: Law;
  revision: LawRevision;
  nodes: LawNode[];
  isSaved: boolean;
  loadedFromStorage: boolean;
  savedAt?: ISODateString;
  requestedAsOf?: string;
}
```

- [ ] **Step 4: Wire `asOf` through the loader**

Replace the body of `loadLawViewerDocument` in `src/app/law-viewer-loader.ts` with:

```ts
export const loadLawViewerDocument = async (
  lawId: string,
  repository: LawRepository = defaultLawViewerRepository,
  storageRepository: StorageRepository = defaultStorageRepository,
  asOf?: string,
): Promise<LawViewerState> => {
  if (lawId.trim() === "") {
    return { status: "error", message: "法令が見つかりません。" };
  }

  if (lawId === offlineDemoLawId) {
    return { status: "offline-unavailable", lawTitle: sampleLawViewerDocument.law.title };
  }

  const savedDocument = await getSavedDocument(storageRepository, lawId);

  try {
    const document = await repository.getLaw(lawId, asOf === undefined ? {} : { asOf });

    return {
      status: "ready",
      law: document.law,
      revision: document.revision,
      nodes: document.nodes,
      isSaved: savedDocument !== undefined,
      loadedFromStorage: false,
      requestedAsOf: asOf,
      savedAt: savedDocument?.savedAt,
    };
  } catch (error) {
    if (!(error instanceof EgovApiError) || error.status !== 404) {
      if (savedDocument !== undefined) {
        return {
          status: "ready",
          law: savedDocument.law,
          revision: savedDocument.revision,
          nodes: savedDocument.nodes,
          isSaved: true,
          loadedFromStorage: true,
          requestedAsOf: asOf,
          savedAt: savedDocument.savedAt,
        };
      }

      // 基準日を指定していて版が無い場合（e-Gov は 400 を返す）は、原因が分かる文言にする。
      if (asOf !== undefined && error instanceof EgovApiError && error.status === 400) {
        return {
          status: "error",
          message: "指定した基準日にはこの法令の版が見つかりません。基準日を変更してください。",
        };
      }

      return {
        status: "error",
        message: "法令を取得できませんでした。ネットワーク接続を確認してください。",
      };
    }

    return { status: "error", message: "法令が見つかりません。" };
  }
};
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `pnpm test src/app/law-viewer-loader.test.ts`
Expected: PASS（既存＋新規すべて）。既存テストは `asOf` 未指定で URL 不変・挙動不変。

- [ ] **Step 6: Run the verification gate**

Run: `pnpm test src/app/law-viewer-loader.test.ts && pnpm typecheck && pnpm lint && pnpm run format:check`
Expected: すべて PASS。

- [ ] **Step 7: Commit**

```bash
git add src/app/law-viewer-sample.ts src/app/law-viewer-loader.ts src/app/law-viewer-loader.test.ts
git commit -m "$(cat <<'EOF'
feat(viewer): 基準日を asof としてローダーへ配線

基準日を getLaw の asof に渡し、解決に使った基準日を状態へ記録する。
基準日指定で版が無い場合は原因の分かるエラー文言にする。

Refs #70

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: ビューワーの配線と表示

**Files:**

- Modify: `src/app/law-viewer-page.tsx`
- Test: `src/app/law-viewer-page.test.tsx`

**Interfaces:**

- Consumes: `useBaseDate`（`./use-base-date`）、`resolveAsOf`（`@/core/settings`）、`loadLawViewerDocument(..., asOf?)`、`LawViewerDocument.requestedAsOf`、`formatIsoDateLabel`（`@/shared/utils/dates`）
- Produces: 表示のみ（新しい公開 API なし）

- [ ] **Step 1: Write the failing tests**

Add to `src/app/law-viewer-page.test.tsx`:

At the top of the test file, make three import/setup changes:

1. Add `act` to the Testing Library import and `afterEach` to the vitest import:

```ts
import { act, render, screen, waitFor, within } from "@testing-library/react";
import { afterEach, describe, expect, it, vi } from "vitest";
```

2. Add the settings-store import to the `@/…` import group:

```ts
import { setBaseDate } from "@/core/settings";
```

3. Add an `afterEach` that clears `localStorage`（`const scrollMocks = setupScrollMocks();` の直後）。基準日はテスト間で共有される `localStorage` に載るため、毎テストでクリアして分離する:

```ts
afterEach(() => {
  localStorage.clear();
});
```

そして `describe("LawViewerPageContent", ...)` の末尾（`renders the study context panel` の後）に次の3テストを追加する:

```ts
it("shows the current-law base date label when no base date is set", () => {
  renderLawViewerContentRoute("/laws/129AC0000000089/articles/1", {
    status: "ready",
    ...sampleLawViewerDocument,
  });

  expect(screen.getAllByText(/基準日 未設定（現行法）/).length).toBeGreaterThan(0);
  expect(screen.getAllByText(/施行日 2026-06-24/).length).toBeGreaterThan(0);
});

it("shows the resolved base date when one is requested", () => {
  renderLawViewerContentRoute("/laws/129AC0000000089/articles/1", {
    status: "ready",
    ...sampleLawViewerDocument,
    requestedAsOf: "2020-06-01",
  });

  expect(screen.getAllByText(/基準日 2020-06-01/).length).toBeGreaterThan(0);
});

it("notes that the base date is not applied to an offline saved body", () => {
  renderLawViewerContentRoute("/laws/129AC0000000089/articles/1", {
    status: "ready",
    ...sampleLawViewerDocument,
    loadedFromStorage: true,
    requestedAsOf: "2020-06-01",
  });

  expect(screen.getByText(/保存版を表示中のため基準日は未反映/)).toBeInTheDocument();
});
```

さらに、完了条件（基準日変更で版が切り替わる）を検証する統合テストを追加する。`describe` の末尾に:

```ts
it("re-resolves the displayed revision when the base date changes", async () => {
  const currentDocument = {
    law: sampleLawViewerDocument.law,
    revision: { ...sampleLawViewerDocument.revision, effectiveDate: "2026-06-24" },
    nodes: sampleLawViewerDocument.nodes,
    raw: {},
  } satisfies LawDocument;
  const olderDocument = {
    law: sampleLawViewerDocument.law,
    revision: {
      ...sampleLawViewerDocument.revision,
      revisionId: "129AC0000000089_20200401_501AC0000000034",
      effectiveDate: "2020-04-01",
    },
    nodes: sampleLawViewerDocument.nodes,
    raw: {},
  } satisfies LawDocument;
  const repository = {
    listLaws: (): Promise<LawListResult> => Promise.reject(new Error("Not used in this test")),
    getLaw: (_lawId: string, query?: { asOf?: string }): Promise<LawDocument> =>
      Promise.resolve(query?.asOf === "2020-06-01" ? olderDocument : currentDocument),
    getLawMetadata: (): Promise<LawMetadata> => Promise.reject(new Error("Not used in this test")),
  } satisfies LawRepository;

  renderLawViewerRoute("/laws/129AC0000000089", repository);

  expect(await screen.findByRole("article", { name: "民法" })).toBeInTheDocument();
  expect(screen.getAllByText(/施行日 2026-06-24/).length).toBeGreaterThan(0);

  act(() => {
    setBaseDate("2020-06-01");
  });

  await waitFor(() => {
    expect(screen.getAllByText(/施行日 2020-04-01/).length).toBeGreaterThan(0);
  });
});
```

（`localStorage` の後片付けは Step 1 で追加した `afterEach` が担う。）

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test src/app/law-viewer-page.test.tsx`
Expected: FAIL（基準日表示が未実装）。

- [ ] **Step 3: Wire the base date into the page**

In `src/app/law-viewer-page.tsx`:

3-1. Extend the domain type import and add the new imports (near the other imports):

```ts
import type { LawNode, LawRevision } from "@/core/domain";
```

（既存の `import type { LawNode } from "@/core/domain";` を上記に置き換える。）

Add after the `@/…` import block（相対 import 群の直前）:

```ts
import { resolveAsOf } from "@/core/settings";
import { formatIsoDateLabel } from "@/shared/utils/dates";
```

Add to the relative imports:

```ts
import { useBaseDate } from "./use-base-date";
```

3-2. Read the base date in `LawViewerPage` and pass `asOf` down:

```ts
export const LawViewerPage = ({
  repository,
  storageRepository = defaultStorageRepository,
}: LawViewerPageProps = {}) => {
  const { article, lawId } = useLawViewerParams();
  const { baseDate } = useBaseDate();
  const asOf = resolveAsOf(baseDate);

  return (
    <LawViewerPageLoader
      key={lawId}
      activeArticleNumber={article}
      asOf={asOf}
      lawId={lawId}
      repository={repository}
      storageRepository={storageRepository}
    />
  );
};
```

3-3. Accept `asOf` in `LawViewerPageLoader`, add it to the effect deps, and forward it to the loader:

```ts
const LawViewerPageLoader = ({
  activeArticleNumber,
  asOf,
  lawId,
  repository,
  storageRepository,
}: {
  activeArticleNumber?: string;
  asOf?: string;
  lawId: string;
  repository?: LawRepository;
  storageRepository: StorageRepository;
}) => {
  const [state, setState] = useState<LawViewerState>({ status: "loading" });

  useEffect(() => {
    let isCurrent = true;

    void loadLawViewerDocument(lawId, repository, storageRepository, asOf).then((nextState) => {
      if (isCurrent) {
        setState(nextState);
      }
    });

    return () => {
      isCurrent = false;
    };
  }, [asOf, lawId, repository, storageRepository]);

  return (
    <LawViewerPageContent
      activeArticleNumber={activeArticleNumber}
      lawId={lawId}
      state={state}
      storageRepository={storageRepository}
    />
  );
};
```

- [ ] **Step 4: Add the base date display helpers and markup**

4-1. Add module-level helpers near `collectTocArticleNumbers`（ファイル下部のヘルパー群）:

```ts
// 表示に使った基準日のラベル。未設定なら現行法である旨を示す。
const formatBaseDateLabel = (state: Extract<LawViewerState, { status: "ready" }>): string =>
  state.requestedAsOf ?? "未設定（現行法）";

// 解決版の施行日ラベル。未施行版など施行日が無い場合は「不明」にする。
const formatEffectiveDateLabel = (revision: LawRevision): string =>
  revision.effectiveDate === undefined ? "不明" : `${revision.effectiveDate} 版`;
```

4-2. Add the toolbar indicator inside the toolbar card. In `LawViewerReadyState`'s returned JSX, insert this block right after the オフライン `</div>`（`{hasArticleError ? ...}` の直前）:

```tsx
<div aria-label="基準日情報" className="grid min-w-0 gap-1 md:w-full" role="group">
  <span className="text-sm font-medium text-foreground">基準日</span>
  <p className="text-sm text-muted-foreground">
    基準日 {formatBaseDateLabel(state)} ・ 施行日 {formatEffectiveDateLabel(state.revision)}{" "}
    <Link className="text-primary underline-offset-4 hover:underline" to="/settings">
      設定で変更
    </Link>
  </p>
</div>
```

4-3. Add the bottom footer note. Insert right after the `<LawDocumentView ... />` closing（本文カラム `<div className="min-w-0 px-4 py-6 md:px-8">` を閉じる `</div>` の直前）:

```tsx
<p className="mt-6 border-t pt-4 text-xs leading-5 text-muted-foreground">
  基準日 {formatBaseDateLabel(state)} ・ 施行日 {formatEffectiveDateLabel(state.revision)} ・
  取得日時 {formatIsoDateLabel(state.revision.fetchedAt)}
  {state.loadedFromStorage && state.requestedAsOf !== undefined
    ? "（保存版を表示中のため基準日は未反映）"
    : ""}
</p>
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `pnpm test src/app/law-viewer-page.test.tsx`
Expected: PASS（新規4テスト＋既存すべて）。

- [ ] **Step 6: Run the verification gate**

Run: `pnpm test && pnpm typecheck && pnpm lint && pnpm run format:check`
Expected: すべて PASS（全スイート）。

- [ ] **Step 7: Commit**

```bash
git add src/app/law-viewer-page.tsx src/app/law-viewer-page.test.tsx
git commit -m "$(cat <<'EOF'
feat(viewer): 基準日で版を解決し表示する

useBaseDate を読み asof を再フェッチへ配線。ツールバーとフッターに
解決に使った基準日・施行日・取得日時を表示し、オフライン保存版では
基準日が未反映である旨を明示する。

Refs #70

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 設定画面の基準日入力

**Files:**

- Create: `src/app/settings-page.tsx`
- Create: `src/app/settings-page.test.tsx`
- Modify: `src/app/pages.tsx`（`SettingsPage` 実装を削除し再エクスポート）

**Interfaces:**

- Consumes: `useBaseDate`（`./use-base-date`）、`earliestBaseDate`・`isValidBaseDate`（`@/core/settings`）、`Input`（`@/shared/ui/input`）
- Produces: `SettingsPage`（`pages.tsx` から従来どおり再エクスポート。`router.tsx` の import は不変）

- [ ] **Step 1: Write the failing test**

Create `src/app/settings-page.test.tsx`:

```ts
import { fireEvent, render, screen } from "@testing-library/react";
import { afterEach, describe, expect, it } from "vitest";

import { getBaseDate } from "@/core/settings";

import { SettingsPage } from "./settings-page";

afterEach(() => {
  localStorage.clear();
});

describe("SettingsPage 基準日", () => {
  it("persists a valid base date", () => {
    render(<SettingsPage />);

    fireEvent.change(screen.getByLabelText("学習年度の基準日"), {
      target: { value: "2020-06-01" },
    });

    expect(getBaseDate()).toBe("2020-06-01");
  });

  it("rejects a base date before the e-Gov lower bound", () => {
    render(<SettingsPage />);

    fireEvent.change(screen.getByLabelText("学習年度の基準日"), {
      target: { value: "2016-01-01" },
    });

    expect(screen.getByRole("alert")).toHaveTextContent("2017-04-01 以降");
    expect(getBaseDate()).toBeUndefined();
  });

  it("clears the base date when emptied", () => {
    localStorage.setItem("surasura:base-date", "2020-06-01");
    render(<SettingsPage />);

    fireEvent.change(screen.getByLabelText("学習年度の基準日"), {
      target: { value: "" },
    });

    expect(getBaseDate()).toBeUndefined();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test src/app/settings-page.test.tsx`
Expected: FAIL（`settings-page` が存在しない）。

- [ ] **Step 3: Create the settings page**

Create `src/app/settings-page.tsx`:

```tsx
import { type ChangeEvent, useId, useState } from "react";

import { earliestBaseDate, isValidBaseDate } from "@/core/settings";
import { Input } from "@/shared/ui/input";

import { useBaseDate } from "./use-base-date";

interface SettingsRow {
  label: string;
  value: string;
}

interface SettingsGroup {
  heading: string;
  rows: SettingsRow[];
}

// 基準日以外はまだ静的表示のグループ。後続 issue で順次実体化する。
const staticGroups: SettingsGroup[] = [
  {
    heading: "表示",
    rows: [
      { label: "文字サイズ", value: "標準" },
      { label: "行間", value: "ゆったり" },
      { label: "テーマ", value: "自動" },
      { label: "既定の表示", value: "読みやすい表示" },
    ],
  },
  {
    heading: "データ",
    rows: [
      { label: "オフライン保存の管理", value: "準備中" },
      { label: "エクスポート / インポート", value: "準備中" },
      { label: "ときどき六法と連携", value: "未接続" },
    ],
  },
];

const StaticSettingsGroup = ({ group }: { group: SettingsGroup }) => (
  <section className="grid gap-2">
    <h2 className="text-xs font-medium tracking-widest text-muted-foreground">{group.heading}</h2>
    <div className="divide-y rounded-md border bg-card">
      {group.rows.map((row) => (
        <div key={row.label} className="flex items-center justify-between px-4 py-3 text-sm">
          <span className="text-foreground">{row.label}</span>
          <span className="text-muted-foreground">{row.value}</span>
        </div>
      ))}
    </div>
  </section>
);

export const SettingsPage = () => {
  const { baseDate, setBaseDate } = useBaseDate();
  const baseDateInputId = useId();
  const baseDateErrorId = useId();
  const [error, setError] = useState<string | undefined>();

  const handleBaseDateChange = (event: ChangeEvent<HTMLInputElement>) => {
    const value = event.target.value;

    if (value === "") {
      setBaseDate(undefined);
      setError(undefined);
      return;
    }

    if (!isValidBaseDate(value)) {
      setError(`基準日は ${earliestBaseDate} 以降を指定してください。`);
      return;
    }

    setError(undefined);
    setBaseDate(value);
  };

  return (
    <section className="mx-auto grid w-full max-w-2xl gap-6 px-5 py-10">
      <h1 className="font-serif text-2xl font-semibold text-foreground">設定</h1>

      <StaticSettingsGroup group={staticGroups[0]} />

      <section className="grid gap-2">
        <h2 className="text-xs font-medium tracking-widest text-muted-foreground">学習</h2>
        <div className="divide-y rounded-md border bg-card">
          <div className="grid gap-2 px-4 py-3 text-sm">
            <label htmlFor={baseDateInputId} className="font-medium text-foreground">
              学習年度の基準日
            </label>
            <Input
              aria-describedby={error === undefined ? undefined : baseDateErrorId}
              aria-invalid={error === undefined ? undefined : true}
              className="w-fit"
              id={baseDateInputId}
              min={earliestBaseDate}
              onChange={handleBaseDateChange}
              type="date"
              value={baseDate ?? ""}
            />
            <p className="text-xs text-muted-foreground">
              未設定のときは現行法（今日時点で施行）を表示します。
            </p>
            {error !== undefined ? (
              <p id={baseDateErrorId} role="alert" className="text-xs leading-5 text-destructive">
                {error}
              </p>
            ) : null}
          </div>
          <div className="flex items-center justify-between px-4 py-3 text-sm">
            <span className="text-foreground">科目プリセット</span>
            <span className="text-muted-foreground">未設定</span>
          </div>
        </div>
      </section>

      <StaticSettingsGroup group={staticGroups[1]} />
    </section>
  );
};
```

- [ ] **Step 4: Re-export from `pages.tsx`**

In `src/app/pages.tsx`:

4-1. Remove the `SettingsRow` interface, `SettingsGroup` interface, `settingsGroups` constant, and the entire `export const SettingsPage = (...) => (...)` block（Read の 127–183 行相当）。

4-2. Add to the re-export block at the bottom（`export { HomePage } ...` の並び）:

```ts
export { SettingsPage } from "./settings-page";
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `pnpm test src/app/settings-page.test.tsx`
Expected: PASS。

- [ ] **Step 6: Run the verification gate**

Run: `pnpm test && pnpm typecheck && pnpm lint && pnpm run format:check`
Expected: すべて PASS（`router.tsx` は `./pages` から `SettingsPage` を引き続き import できる）。

- [ ] **Step 7: Commit**

```bash
git add src/app/settings-page.tsx src/app/settings-page.test.tsx src/app/pages.tsx
git commit -m "$(cat <<'EOF'
feat(settings): 学習年度の基準日を設定画面で編集可能にする

SettingsPage を独立ファイルへ抽出し、基準日を日付入力にする。
2017-04-01 未満は保存せずエラーを表示し、空でクリアできる。

Refs #70

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: ADR（API 検証結果の記録）

**Files:**

- Create: `docs/adr/2026-07-09-egov-asof-server-side-resolution.md`

- [ ] **Step 1: Write the ADR**

Create `docs/adr/2026-07-09-egov-asof-server-side-resolution.md`:

```markdown
# e-Gov API v2 の asof をサーバー側の版解決として利用する

Date: 2026-07-09

## Status

Accepted

## Context

Issue #70 では、学習年度の基準日に基づき「施行日 ≤ 基準日 の最新版」で法令を表示する。
[改正・基準日の参照設計](../superpowers/specs/2026-07-07-revision-and-asof-design.md) の 8 章は、e-Gov API v2 が版指定取得を提供しない場合に、取得のたびに前回スナップショットと指紋比較する縮退方式を採ると定めていた。実装着手時に API 仕様を検証する必要があった。

## Decision

2026-07-09 に本番 API（民法 `129AC0000000089`）へ実リクエストして次を確認し、`asof` によるサーバー側版解決を採用する。

- `GET /law_data/{lawId}?asof=YYYY-MM-DD` は「施行日 ≤ asof の最新版」を返す（`asof=2020-06-01` → 2020-04-01 施行版）。
- `asof` 未指定は現行施行版を返す（今日の asof と同結果）。未来日は未施行の最新予定版を返す。
- `asof` の下限は **2017-04-01**。それ未満は HTTP 400（`code=400044`）。

したがって参照設計 8 章の縮退方式（指紋差分）は採らない。クライアントは基準日を `asof` として渡すだけでよく、施行日比較は実装しない。

## Consequences

- 基準日の版解決は `EgovLawRepository.getLaw(lawId, { asOf })` への配線に集約され、クライアントに版比較ロジックを持たない。
- 基準日入力は 2017-04-01 を下限として検証する（`src/core/settings/base-date.ts` の `earliestBaseDate`）。下限未満や不正日は保存しない。
- オフライン保存法令は法令ごとに 1 版のみ保持するため、オンライン解決版と食い違い得る。この再同期（「更新あり」）は #71/#20 の責務とし、本 issue では表示上のラベリングに留める。
- 条文指紋による改変検知は保存物（ブックマーク・メモ・カード）向けの別機能として #71/#20 で扱う。
```

- [ ] **Step 2: Run the verification gate**

Run: `pnpm run format:check docs/adr/2026-07-09-egov-asof-server-side-resolution.md`
Expected: PASS。落ちる場合は `pnpm run format docs/adr/2026-07-09-egov-asof-server-side-resolution.md` で整形してから再確認する。

- [ ] **Step 3: Commit**

```bash
git add docs/adr/2026-07-09-egov-asof-server-side-resolution.md
git commit -m "$(cat <<'EOF'
docs(adr): e-Gov asof のサーバー側版解決を記録

asof が「施行日 ≤ asof の最新版」を返すこと、下限 2017-04-01 を検証し、
縮退方式を不採用と決めた根拠を残す。

Refs #70

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: 統合検証と証跡

**Files:** なし（検証のみ）

- [ ] **Step 1: Full suite gate**

Run: `pnpm test && pnpm typecheck && pnpm lint && pnpm run format:check`
Expected: すべて PASS。

- [ ] **Step 2: Manual E2E with playwright-cli**

Run: `pnpm dev`（別ターミナル）。`playwright-cli` で以下を確認する:

1. `/laws/129AC0000000089` を開き、ツールバーに「基準日 未設定（現行法）・施行日 …版」、フッター注記に取得日時が出る。
2. `/settings` で基準日に `2020-06-01` を入力する。
3. `/laws/129AC0000000089` に戻り、表示版の施行日が 2020-04-01 版へ切り替わり、ツールバーが「基準日 2020-06-01」を示す。
4. 設定で基準日を空にし、現行法に戻ることを確認する。

各ステップでスクリーンショットを撮影する（基準日切替の前後を含む）。

- [ ] **Step 3: Attach evidence to the PR**

`github-upload-image` スキルでスクリーンショットを PR 本文へ添付する（CLAUDE.md）。

- [ ] **Step 4: Draft PR / PR**

Draft PR を作成する（未完事項が無ければ通常 PR）。本文に `Close #70` を付け、`Blocking: #71, #20` を記す。作成後、`SlashNephy` を Assign する。

---

## Self-Review

**1. Spec coverage:**

- spec 3 章（API 検証）→ Task 6（ADR）。
- spec 4.1（設定モジュール）→ Task 1。
- spec 4.2（フック）→ Task 2。
- spec 4.3（ローダー配線・再フェッチ）→ Task 3・Task 4（Step 3）。
- spec 4.4（設定画面 date 入力）→ Task 5。
- spec 4.5（ツールバー・フッター表示）→ Task 4（Step 4）。
- spec 5（データフロー）・完了条件 → Task 4 の統合テスト（基準日変更で版切替）＋ Task 1 の `resolveAsOf`（未設定→現行）。
- spec 6（エラー・エッジ）→ Task 3（400 エラー文言）＋ Task 4（施行日「不明」・オフライン注記）。
- spec 7（キャッシュ・オフライン）→ Task 4（オフライン注記）＋ Task 6（ADR の Consequences）。
- spec 8（テスト計画）→ 各タスクの Step 1。

**2. Placeholder scan:** プレースホルダーなし。各ステップに実コードと期待出力を記載済み。

**3. Type consistency:** `requestedAsOf?: string` を `LawViewerDocument`（Task 3）で定義し、ローダー（Task 3）・表示ヘルパー（Task 4）で同名参照。`asOf`（loader 引数・`LawDataQuery.asOf`）と `resolveAsOf`/`getBaseDate` の戻り `string | undefined` が一貫。`getLaw(lawId, { asOf })` は既存 `LawDataQuery` に一致。

# 初期ドキュメントとREADME Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Issue #11 の完了条件として、README と docs からプロダクト方針、設計、タスク分割を追えるようにする。

**Architecture:** 既存の `docs/design-doc.md` を canonical な設計文書として維持し、README は初見向けの入口、`docs/tasks.md` は GitHub Issue 群の索引、`docs/adr/` は将来の設計判断の置き場に分ける。設計そのものは大きく変えず、現状の実装基盤と追跡導線を補う。

**Tech Stack:** Markdown, Prettier, GitHub Issues.

---

### Task 1: Documentation Entry Points

**Files:**

- Modify: `README.md`
- Modify: `docs/design-doc.md`
- Create: `docs/tasks.md`
- Create: `docs/adr/README.md`
- Create: `docs/superpowers/plans/2026-07-05_初期ドキュメントとREADME.md`

- [ ] **Step 1: Expand README**

Add project positioning, key features, repository navigation, setup, and quality-check sections.

- [ ] **Step 2: Refresh design doc metadata and links**

Update the date, current stack, M0 state, and documentation links without changing the product scope.

- [ ] **Step 3: Add task index**

Create `docs/tasks.md` with parent/child Issue groups, phase mapping, dependency notes, and current status.

- [ ] **Step 4: Add ADR placeholder**

Create `docs/adr/README.md` that defines the ADR naming rule and clarifies that no ADR has been recorded yet.

- [ ] **Step 5: Run formatting check**

Run:

```bash
pnpm run format:check
```

Expected: PASS.

- [ ] **Step 6: Run independent review**

Run:

```bash
pnpm run review:antigravity
```

Expected: PASS, or skip when `agy` is unavailable.

- [ ] **Step 7: Commit**

Run:

```bash
git add README.md docs/design-doc.md docs/tasks.md docs/adr/README.md docs/superpowers/plans/2026-07-05_初期ドキュメントとREADME.md
git commit -m "docs: 初期ドキュメントを整備する"
```

Expected: commit succeeds.
